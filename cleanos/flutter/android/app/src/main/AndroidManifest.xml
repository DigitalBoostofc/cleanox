<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Slice B4 (doc 09) — tracking GPS "a caminho". Tudo atrás de
         Env.trackingEnabled; com a flag OFF nada é exercitado, mas as permissões
         ficam declaradas para o APK de produção. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Câmera/galeria (evidências, Slice B2) — image_picker usa o seletor do
         sistema; a permissão de câmera é solicitada em runtime ao tirar foto. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- A-02 (LGPD): sem backup do app data (nuvem e device-to-device). O
         diretório de documentos guarda fotos de evidência (interior da casa do
         cliente) até o upload — nada disso pode subir pro backup do Google da
         conta pessoal do profissional. Em API 31+, allowBackup=false também
         desativa a transferência d2d. -->
    <application
        android:label="cleanos"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Deep-link nativo: entrega o path do App Link ao go_router (em vez
                 de só relançar o app). Necessário p/ o link "Nova OS" (WhatsApp)
                 abrir a rota /app/os/:id. -->
            <meta-data
              android:name="flutter_deeplinking_enabled"
              android:value="true" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- App Link (doc 09 §6.3): https://app.cleanox.com.br/app/os/<id>
                 abre o app direto na OS. autoVerify exige o assetlinks.json
                 publicado em /.well-known/ com o SHA-256 da chave de release.
                 Escopo restrito a /app/os/ — o restante do domínio (web app, /api)
                 continua abrindo no navegador. Sem o app instalado, o mesmo link
                 abre a rota no app web (fallback gracioso). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="app.cleanox.com.br"
                    android:pathPrefix="/app/os/" />
            </intent-filter>
        </activity>
        <!-- Slice B4 — foreground service do flutter_foreground_task (tracking GPS).
             Warning: não mudar o nome do serviço. foregroundServiceType=location. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
