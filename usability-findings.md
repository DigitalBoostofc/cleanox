# Usability Findings — QA Vision Lab

> Contrato compartilhado. Cada worker fica NA SUA faixa de ID. Append-only:
> nunca reescreva findings de outro worker. Status: aberto → corrigido →
> verificado | reaberto (| precisa-decisão).

## F-301 | categoria: funcional | severidade: média | status: corrigido
- Tela: cleanos/web/src/pages/app/MeusServicos.tsx:377-386 (e Mapa.tsx:51, Perfil.tsx:63)
- Passos: 1) Uma OS é atribuída ao profissional com data_hora de um dia anterior (ex.: ontem) e ele não a iniciou/concluiu. 2) Hoje o profissional abre o app.
- Esperado: OS abertas (atribuida/em_andamento) que ficaram pra trás deveriam continuar visíveis pra serem iniciadas/concluídas.
- Observado: A consulta busca SÓ `data_hora >= todayStart` (seção Hoje) e `data_hora >= tomorrowStart` (Próximos). Não existe limite inferior que recupere OS passadas em aberto → qualquer OS com data_hora < hoje some por completo da tela do profissional (não há como concluir). No Mapa o filtro `status='em_andamento' && data_hora >= todayStart` também faz um serviço em andamento iniciado no dia anterior (ou que cruzou a virada do dia UTC) sumir do mapa, escondendo o endereço liberado. Relacionado a [F-302].

## F-302 | categoria: funcional | severidade: média | status: corrigido
- Tela: cleanos/web/src/lib/collections.ts:215 (getUtcDayBounds) consumido por MeusServicos.tsx:374, Mapa.tsx:45, Perfil.tsx:59
- Passos: 1) Estar no horário entre ~21:00 e 23:59 BRT. 2) Abrir "Meus serviços".
- Esperado: A seção "Hoje" deve refletir o dia corrente em horário local (BRT) e bater com o cabeçalho `todayLabel` (que usa data local).
- Observado: getUtcDayBounds calcula os limites do dia pelo **calendário UTC** (now.getUTCFullYear/Month/Date), enquanto o cabeçalho usa `new Date().toLocaleDateString('pt-BR')` (local). Como BRT = UTC-3, a partir das 21:00 BRT o "dia UTC" já virou: a janela "Hoje" passa a trazer os serviços do **próximo dia** (madrugada/manhã seguinte) enquanto o rótulo ainda mostra a data de hoje. Resultado: bucket "Hoje" desalinhado do rótulo e dos serviços que o profissional espera ver à noite. Mesma convenção UTC é usada em isOsToday (MeusServicos.tsx:28), então a inconsistência se propaga.

## F-303 | categoria: funcional | severidade: baixa | status: corrigido
- Tela: cleanos/web/src/lib/pb.ts:9 + cleanos/web/src/pages/app/MeusServicos.tsx:369-443
- Passos: 1) Abrir "Meus serviços" (fetchOS dispara). 2) Antes do fetch resolver, chega um evento realtime de update de uma OS (ex.: admin altera status). 3) O fetch resolve depois.
- Esperado: O estado deve refletir a atualização mais recente.
- Observado: `pb.autoCancellation(false)` está global e nenhum fetch (fetchOS/fetchActive/fetchStats) tem guarda de geração/cleanup (flag "ignore"/AbortController). O handler realtime faz setTodayOS(map) com o record novo; se o fetch in-flight (capturado ANTES do evento) resolver depois, ele sobrescreve a lista inteira com `todayRes.items` stale, revertendo a atualização realtime. Janela estreita e se auto-corrige no próximo evento/refresh, mas é uma corrida real introduzida por desligar o autoCancellation sem dedupe por requestKey.

## F-304 | categoria: funcional | severidade: baixa | status: corrigido
- Tela: cleanos/web/src/lib/pb.ts (sem authRefresh em todo o app) — sintoma em MeusServicos.tsx:390, Mapa.tsx:55, Perfil.tsx:77
- Passos: 1) Manter sessão aberta até o token persistido expirar. 2) Tentar carregar serviços.
- Esperado: Token deveria ser renovado proativamente (authRefresh) ou, ao expirar, a camada de dados deveria limpar a auth e levar ao login.
- Observado: Não há nenhuma chamada a `pb.collection('users').authRefresh()` em lugar nenhum do app. O token persistido nunca é renovado. Quando expira, `pb.authStore.onChange` NÃO dispara (não há mutação do store só pela passagem do tempo), então a UI continua achando que está logada enquanto todo fetch falha com 401 — exibido ao profissional como genérico "Erro 401: tente novamente." (extractApiError), sem re-autenticação nem redirect a partir da camada de dados. (Escopo do guard/redirect pode pertencer ao outro reviewer; aqui o ponto é a ausência de refresh/handling no pb.ts.)

## F-201 | categoria: funcional | severidade: média | status: corrigido
- Tela: cleanos/web/src/pages/painel/Agenda.tsx:131-149
- Passos: 1) Abrir Agenda 2) Clicar rapidamente em ‹ / › (ou trocar Dia/Semana/Mês) várias vezes seguidas
- Esperado: a grade sempre mostra os dados do período atualmente selecionado.
- Observado: `load` (useCallback deps `[view, anchor]`) dispara a cada mudança e NÃO tem guarda de cancelamento. Duas requisições em voo podem resolver fora de ordem e a resposta antiga sobrescrever a nova (`setOsData`), exibindo eventos do período errado. Compare com Dashboard.tsx que usa um flag `cancelled`. Race de fetch.

## F-202 | categoria: funcional | severidade: média | status: corrigido
- Tela: cleanos/web/src/pages/painel/Financeiro.tsx:51-76
- Passos: 1) Abrir Financeiro 2) Clicar rápido em ‹/› (mês anterior/próximo) repetidamente
- Esperado: KPIs e lançamentos sempre refletem o mês exibido no label.
- Observado: `load` (deps `[year, month]`) recarrega sem guarda de cancelamento. Resposta de um mês antigo pode chegar depois e sobrescrever o mês atual (`setOsAll`/`setProfissionais`), mostrando faturamento/repasses de outro período sob o label errado. Race de fetch.

## F-203 | categoria: funcional | severidade: média | status: corrigido
- Tela: cleanos/web/src/pages/painel/Financeiro.tsx:26-32 (getMonthBounds)
- Passos: 1) Concluir uma OS às 21:00–23:59 do último dia do mês (horário local BRT, UTC-3) 2) Abrir Financeiro no mês corrente
- Esperado: a OS conta no mês em que foi concluída no horário local.
- Observado: `getMonthBounds` monta os limites como string UTC ingênua (`${year}-${month+1}-01 00:00:00`) a partir do mês LOCAL (`now.getMonth()`), sem aplicar o offset. Como `data_hora` é UTC, o limite "2025-07-01 00:00:00" equivale a 2025-06-30 21:00 BRT — OSs de 21:00–23:59 do dia 30 caem no mês seguinte. "Recebido no mês" / "A repassar" ficam errados na virada de mês. Bug de fuso/off-by-offset. Mesma classe de [F-302]/[F-204].

## F-204 | categoria: funcional | severidade: média | status: corrigido
- Tela: cleanos/web/src/pages/painel/Dashboard.tsx:37 (usa getUtcDayBounds)
- Passos: 1) Ter OS concluídas/agendadas às 21:00–23:59 BRT 2) Abrir o Dashboard
- Esperado: os KPIs de "Hoje" e "Faturamento hoje" cobrem o dia LOCAL exibido no cabeçalho (linha 94 usa `toLocaleDateString` local).
- Observado: o filtro usa `getUtcDayBounds()`, que gera limites de meia-noite UTC pura (sem offset). A janela [00:00 UTC, 00:00 UTC+1) corresponde a [21:00, 21:00] no horário local BRT. Logo, atendimentos das 21:00–23:59 de hoje caem em "amanhã" e os de ontem 21:00–23:59 aparecem hoje. KPIs/Faturamento do dia desalinhados do dia mostrado; Agenda faz a conversão correta via toUtcStr. Sintoma no painel (contraparte de [F-302] no app; causa-raiz do helper é escopo de lib/outro reviewer).

## F-205 | categoria: usabilidade | severidade: média | status: corrigido
- Tela: cleanos/web/src/pages/painel/Clientes.tsx:178-185 (toggleAtivo)
- Passos: 1) Com erro de permissão/rede, clicar no chip Ativo/Inativo de um cliente
- Esperado: feedback de erro ao usuário quando a atualização falha.
- Observado: o `catch {}` é vazio. O comentário diz "silently show inline" mas NADA é exibido. Em falha, o chip simplesmente não muda e o usuário não recebe nenhum aviso (erro engolido). Não há estado de erro renderizado para essa ação.

## F-206 | categoria: funcional | severidade: média | status: corrigido
- Tela: cleanos/web/src/pages/painel/OrdensServico.tsx:261-273 + 581 (handleAtribuir / OSDetail no modal "Ver detalhes")
- Passos: 1) Abrir uma OS pelo olho (Ver detalhes) 2) Selecionar um profissional e clicar "Atribuir"
- Esperado: após atribuir, o detalhe reflete o novo profissional e o novo status ("Atribuída").
- Observado: `handleAtribuir` faz `update` + `load()`, que recria a lista `ordens`, mas `selectedOS` continua apontando para o objeto antigo. O modal renderiza `<OSDetail os={selectedOS}>`, então "Atribuído" segue mostrando "—"/valor antigo e o badge de status segue "Agendada" até fechar e reabrir o modal. Estado dessincronizado (selectedOS não atualizado após o refetch).

## F-207 | categoria: funcional | severidade: baixa | status: corrigido
- Tela: cleanos/web/src/pages/painel/Avaliacoes.tsx:81-116 (loadReviews / handleToggle)
- Passos: 1) Abrir o acordeão do profissional A 2) Imediatamente abrir o do profissional B
- Esperado: cada acordeão mostra as avaliações do seu próprio profissional.
- Observado: `loadReviews` não vincula a resposta ao `openId` corrente. Se a chamada de A (reset=true) resolver depois da de B, ela sobrescreve `reviews`/`reviewsTotal` enquanto B está aberto, exibindo avaliações de A sob o nome de B. Race de fetch sem guarda.

## F-208 | categoria: funcional | severidade: baixa | status: corrigido
- Tela: cleanos/web/src/pages/painel/PainelLayout.tsx:60-61
- Passos: (estático) Navegar entre páginas do painel
- Esperado: o título do topo deveria derivar do estado de rota do router (useLocation).
- Observado: `currentTitle = PAGE_TITLES[window.location.pathname]` lê `window.location` diretamente em vez de `useLocation()`. Funciona na prática porque `<Routes>` re-renderiza o layout na navegação, mas é frágil: não está inscrito no estado do router e quebra com qualquer divergência (trailing slash/querystring/re-render sem navegação), podendo exibir título obsoleto ou o fallback "Painel".

## F-209 | categoria: visual | severidade: baixa | status: corrigido
- Tela: cleanos/web/src/pages/painel/Agenda.tsx:72-77 (hourSlot)
- Passos: 1) Criar OS com horário fora de 07:00–21:00 (ex.: 23:00 ou 06:00) 2) Abrir Agenda em Dia/Semana
- Esperado: o evento aparece no horário correto (ou claramente sinalizado fora da faixa).
- Observado: `hourSlot` faz clamp para HOURS[0]=7 ou HOURS[última]=21. Um atendimento das 23:00 é renderizado na linha das 21:00 (com o rótulo de hora real 23:00) e um das 06:00 na linha das 07:00 — posicionamento enganoso/empilhamento no slot de borda.

## F-210 | categoria: usabilidade | severidade: baixa | status: corrigido
- Tela: cleanos/web/src/pages/painel/Usuarios.tsx:104-160, 290-295
- Passos: 1) Logado como admin, editar a si mesmo e mudar o papel para gerente/profissional, OU clicar excluir no próprio usuário
- Esperado: proteção contra auto-rebaixamento/auto-exclusão (que pode remover o acesso admin do sistema).
- Observado: não há guarda comparando `editing.id`/`deleteTarget.id` com o usuário logado. Admin pode se rebaixar (perdendo Financeiro/WhatsApp/excluir) ou se autoexcluir sem nenhum aviso. Footgun de permissão/UX.

## F-401 | categoria: funcional | severidade: alta | status: corrigido  [CRÍTICO ANTI-DESVIO]
- Tela: cleanos/pb/pb_hooks/main.pb.js:128-160 (cron `cleanStaleEndereco`) × cleanos/pb/pb_hooks/os_logic.js:77-90 (`manageEndereco`) chamado por main.pb.js:34 (`onRecordUpdate`)
- Passos (servidor `serve`, hooks carregados): 1) OS fica `em_andamento` (profissional tocou Iniciar, liberou `endereco_liberado` com o endereço completo do cofre). 2) O profissional NUNCA conclui — a OS atravessa a virada do dia e fica "eternamente em_andamento". 3) Às 03:05 o cron roda para re-restringir o endereço.
- Esperado: o cron limpa `endereco_liberado` das OS `em_andamento` cujo dia (BRT) já passou — mitigação documentada em README §11.3 ("Endereço retido em OS eterna em_andamento → Cron limpa diariamente"). O endereço deve sumir do histórico/visão do profissional.
- Observado: o cron faz `rec.set("endereco_liberado","")` e em seguida `$app.save(rec)` (main.pb.js:146-147). Esse save dispara o hook de MODELO `onRecordUpdate` → `manageEndereco`, que como o `status` continua `em_andamento` (o cron não muda o status) RE-PREENCHE `endereco_liberado = buildEndereco(cliente)` (os_logic.js:78-86) ANTES do `e.next()` persistir. Ou seja, a limpeza do cron é desfeita no mesmo save: o endereço completo do cliente NUNCA é re-restringido e permanece exposto ao profissional indefinidamente — exatamente o vazamento anti-desvio que o cron deveria fechar. O design é autocontraditório: `manageEndereco` impõe "em_andamento SEMPRE tem endereço" em todo save, então é impossível limpar via `$app.save` sem também sair de `em_andamento` (ou usar update raw sem hook). Reproduz via API: criar/atribuir OS, profissional PATCH `status=em_andamento` (no dia), não concluir, esperar o cron (ou simular o save server-side) → `GET /ordens_servico/records/<id>` como profissional continua trazendo `endereco_liberado` preenchido. NOTA SECUNDÁRIA: a expressão `"5 3 * * *"` roda no fuso LOCAL do processo PocketBase; o comentário "03:05 UTC = 00:05 BRT" só vale se a VPS estiver em UTC — em VPS BRT dispararia 03:05 BRT. (Moot enquanto o cron for inócuo, mas relevante ao corrigir.)
- (resolvedor) Correção: a causa-raiz (manageEndereco re-preenchendo no save do cron) JÁ fora fechada em c7ca1c9: `manageEndereco` (os_logic.js) só preenche `endereco_liberado` na TRANSIÇÃO para `em_andamento` (`oldStatus !== 'em_andamento'`); num save sem transição (caso do cron) retorna sem mexer, então a limpeza "cola". Confirmei isso em runtime (a entrada do contrato refletia o estado deployado antigo, pré-c7ca1c9). Restava a NOTA SECUNDÁRIA do fuso: o cron `"5 3 * * *"` rodava no TZ local do processo. Fix desta rodada: cron passa a `"5 * * * *"` (de hora em hora, minuto :05) — a virada do dia BRT é coberta em ~1h independ. do TZ da VPS; o corte por DIA BRT (`diaBRT < todayBRT`) + save idempotente garante que rodadas extras não tocam OS do dia corrente. Branch `fix/qa-os-repasse-endereco`, commit 9afbfa2 (a partir de main; NÃO mergeado). Arquivo: cleanos/pb/pb_hooks/main.pb.js. Validação: `node --check` OK; runtime em CÓPIA descartável do pb_data (PB 0.39.4, sem envs de webhook): OS `em_andamento` de dia passado salva com `endereco_liberado=''` PERMANECE '' após o save (não re-preenche); OS `em_andamento` do dia corrente recebe o endereço na transição. 2/2 asserts verdes (parte do mesmo runner 6/6 do F-002).
- (resolvedor) precisa-decisão: o default seguro implementado faz a OS presa eternamente `em_andamento` apenas PERDER o endereço (cron limpa). PERGUNTA AO DONO: além de perder o endereço, o cron deve também TRANSICIONAR essa OS estagnada para um estado pausado/cancelado (e qual)? Hoje ela continua `em_andamento` indefinidamente, só sem endereço. Não bloqueia o fix — é evolução de produto.

## F-402 | categoria: funcional | severidade: média | status: corrigido
- Tela: cleanos/pb/pb_hooks/os_logic.js:57-63 (`syncDenormalized`) e :85 (`manageEndereco`) — lookup de `clientes` sem try/catch; contraste com o lookup de `servicos` (:66-72) que É protegido
- Passos: 1) Uma OS passa a referenciar um `cliente` inexistente (ex.: registro removido/órfão por import, manutenção via SQL, ou inconsistência de migração). 2) Qualquer gravação na OS dispara os hooks de modelo (API, seed em serve, Admin UI, rota /a-caminho, /ratings/ingest).
- Esperado: a denormalização degrada com segurança (como já faz para `servico`: try/catch ignora snapshot se o serviço sumiu) e a operação prossegue/aborta de forma controlada.
- Observado: `syncDenormalized` faz `const c = app.findRecordById("clientes", cid)` sem try/catch e logo usa `c.get(...)`; `manageEndereco` (status em_andamento) idem em :85. Se o cliente não existir, `findRecordById` lança e o erro sobe pelo hook de modelo → TODO save da OS estoura (500/400), inclusive um admin tentando CANCELAR ou corrigir a OS para sair do estado ruim (a correção também passa pelo mesmo hook). A assimetria com o lookup de `servico` (protegido) evidencia o gap defensivo. Severidade média (alcance limitado por `cliente` ser required + integridade relacional, mas o impacto é a OS ficar travada para qualquer edição). Robustez/erro não-engolido onde deveria degradar.
- (resolvedor) Correção: try/catch adicionado ao lookup de `clientes` em `syncDenormalized` e `manageEndereco` (os_logic.js:95,99-105,232-237), espelhando o tratamento já existente para `servico` — cliente órfão não trava o save, degrada silenciosamente.

## F-403 | categoria: funcional | severidade: média | status: corrigido
- Tela: cleanos/pb/pb_hooks/main.pb.js:88-92 + os_logic.js:131-147 (`guardOrdemUpdateRequest` só roda em `onRecordUpdateRequest`) × cleanos/pb/pb_migrations/1700000001_init_collections.js:207 (`createRule = ADMIN_GERENTE`)
- Passos: 1) Logar como GERENTE. 2) `POST /api/collections/ordens_servico/records` com `{ cliente, servico, data_hora, status:"concluida", valor_pago:1, forma_pagamento:"pix_maquininha", repasse_status:"pago", repasse_valor:9999 }`.
- Esperado: o controle de segregação financeira "apenas admin marca/altera repasse" (README §3, §6; spec §4 painel) deve valer em qualquer caminho — gerente nunca consegue setar `repasse_status`/`repasse_valor`.
- Observado: a trava de repasse-só-admin existe SOMENTE no guard de UPDATE (os_logic.js:137-145 bloqueia gerente que altera `repasse_status`/`repasse_valor`). Não há guard de CREATE — não existe `onRecordCreateRequest` para `ordens_servico` (main.pb.js só registra `onRecordCreate` de MODELO, que não checa papel/repasse). Logo o gerente pode CRIAR uma OS já com `repasse_status="pago"` e `repasse_valor` arbitrário, injetando no Financeiro um registro "já repassado" sem passar pelo admin — fura a segregação de funções documentada. (O `onRecordCreate` de modelo só impõe `assertPaymentIfConcluida`, que o gerente satisfaz informando o pagamento.) Correção: replicar a trava de repasse num `onRecordCreateRequest` de `ordens_servico`.
- (resolvedor) Correção: `onRecordCreateRequest` para `ordens_servico` adicionado em main.pb.js:89-102 — replica a trava de repasse-só-admin no caminho CREATE, bloqueando gerente/profissional de setar `repasse_status`/`repasse_valor` ao criar uma OS.

## F-001 | categoria: funcional | severidade: média | status: corrigido
- Tela: cleanos/pb/pb_migrations/1700000002_seed.js:72-80 (mkServico, sem checagem por nome) × cleanos/pb/pb_migrations/1700000002_catalog_prod.js:15+ (insere o mesmo catálogo) — sintoma na UI: modal "Nova OS" (OrdensServico.tsx), select SERVIÇO
- Passos: 1) Login admin → Ordens de Serviço → "+ Nova OS". 2) Abrir o dropdown SERVIÇO.
- Esperado: catálogo com 7 serviços únicos (Sofá 2/3 lugares, Poltrona, Colchão solteiro/casal, Cadeira, Tapete).
- Observado: o dropdown lista CADA serviço DUAS vezes (14 itens; "Cadeira", "Colchão casal", "Poltrona", etc. duplicados com IDs diferentes). Confirmado via API: GET /api/collections/servicos/records → totalItems=14, apenas 7 nomes únicos. Causa: duas migrations com o mesmo prefixo `1700000002_` semeiam o catálogo — `seed.js` cria via mkServico SEM idempotência, e `catalog_prod.js` cria de novo (sua checagem "pula se já existir pelo nome" não cobre o que o seed insere, ou roda antes). Resultado: operador escolhe entre serviços visualmente idênticos sem saber qual é o "certo"; preço/snapshot podem divergir. Em produção polui o catálogo permanentemente.
- (resolvedor) Correção: migration 1700000012_dedup_servicos.js agrupa por `nome`, mantém o registro mais antigo de cada grupo e deleta os duplicados — seed idempotente (seed.js:64-75 já protege por nome via try/catch). Idempotente: sem duplicatas, não faz nada.

## F-002 | categoria: funcional | severidade: alta | status: corrigido
- Tela: cleanos/web/src/pages/painel/Financeiro.tsx:80 (filtro `repasse_status === 'pendente'`) × cleanos/pb/pb_hooks/os_logic.js (NENHUM hook seta `repasse_status='pendente'` ao concluir) — seed-only em pb_migrations/1700000002_seed.js:171
- Passos: 1) Concluir uma OS pelo fluxo REAL (profissional Inicia → registra pagamento → Conclui; ou admin transiciona até concluida). 2) Login admin/gerente → Financeiro → seção "A repassar ao profissional".
- Esperado: toda OS concluída com pagamento registrado entra na fila "A repassar ao profissional" (repasse pendente) até o admin marcar como repassado.
- Observado: o estado "a repassar" depende de `repasse_status === 'pendente'`, mas NADA no fluxo de conclusão seta esse valor. O hook de conclusão (os_logic.js:218 assertPaymentIfConcluida + triggerRatingWebhookIfConcluida + manageEndereco) nunca grava `repasse_status`. O campo é `select ["pendente","pago"]` (init_collections.js:188) e fica VAZIO ('') por padrão numa OS concluída de verdade. Só as OS do SEED aparecem na fila porque o seed hardcoda `repasse_status:"pendente"` (seed.js:171). Consequência em produção: depois que um profissional conclui um serviço real, a OS NUNCA aparece em "A repassar" — o admin não tem como marcar/efetuar o repasse pelo sistema e o profissional não é pago pelo fluxo. KPI "A repassar" e a contagem de pendentes também ficam subnotificados. Prova: OS concluída via API/fluxo tem `repasse_status=''`; Financeiro mostra "A repassar R$0 / 0 pendentes" mesmo havendo OS concluída não repassada.
- (resolvedor) Correção: causa raiz = nenhum hook setava `repasse_status` ao concluir. O caminho update→concluida já fora coberto em c7ca1c9 (`setRepasseIfConcluida` no `onRecordUpdate`), MAS o caminho **create-as-concluida** (OS nascendo já `concluida` — admin/gerente lançando OS finalizada, import) continuava sem repasse, pois a função retornava cedo no create (`if (!orig) return`). Fix: `setRepasseIfConcluida` (os_logic.js) reescrita para cobrir os DOIS caminhos — transição x→concluida E create-as-concluida — preenchendo `repasse_status='pendente'` (+`repasse_valor=valor_pago` como default) só quando vazio (NUNCA sobrescreve `'pago'`/`'pendente'` manual) e com `valor_pago>0`; e `onRecordCreate` (main.pb.js) passou a chamá-la. Branch `fix/qa-os-repasse-endereco`, commit 8af5104 (a partir de main; NÃO mergeado). Arquivos: cleanos/pb/pb_hooks/os_logic.js, cleanos/pb/pb_hooks/main.pb.js. Validação: `node --check` OK nos 2 hooks; runtime numa CÓPIA descartável do pb_data (PB 0.39.4, porta 8091, sem envs de webhook → nada de WhatsApp/n8n): create-as-concluida → `repasse='pendente'` (repasse_valor=90); update→concluida → `repasse='pendente'`+repasse_valor=120; OS criada já com `repasse='pago'` permanece `'pago'`. 6/6 asserts verdes. ATENÇÃO ao mergear: PR #17 (fix/qa-servicos-os-round) também edita os_logic.js (F-010, função `fillServiceSnapshot`) — funções distintas, conflito pequeno e gerenciável.

## F-003 | categoria: visual | severidade: baixa | status: inválido (não reproduz)
- Tela: app do profissional (/app, /app/mapa, /app/perfil)
- NOTA: descartado pelo próprio navegador. O "overflow / botões cortados" foi ARTEFATO do resize de janela em andamento (screenshot capturado a 1512px enquanto o CSS ainda estava calculado para o alvo de 390px). Re-testado em viewport estável de 1536px (ss_5328bwefy): o /app tem max-width, conteúdo centralizado, botões inteiros/clicáveis e bottom-nav correta. Não há bug. Mantido só como registro para não recriar.

## F-004 | categoria: usabilidade | severidade: baixa | status: corrigido
- Tela: cleanos/web/src/pages/painel/WhatsApp.tsx:44-54 (extractWAError, linha 48) — evidência screenshot ss_9382i6c0k (painel → WhatsApp)
- Passos: 1) Login admin → WhatsApp. 2) Clicar "Conectar WhatsApp" sem UAZAPI configurada (ambiente sem integração). 3) A request POST /api/cleanos/whatsapp/connect retorna 400.
- Esperado: mensagem de erro amigável em PORTUGUÊS (app é 100% PT-BR), ex.: "Não foi possível conectar ao WhatsApp. Verifique a configuração e tente novamente."
- Observado: o card mostra "Something went wrong while processing your request." — mensagem genérica padrão do PocketBase, EM INGLÊS, num app inteiramente em português. Causa: extractWAError (linha 48) retorna `data.message` cru da resposta; como o PB sempre preenche `message`, o fallback PT-BR da linha 50 (`Erro {status}. Tente novamente.`) nunca é alcançado para erros 400 genéricos do servidor. O botão "Tentar novamente" e o estado "Desconectado" funcionam corretamente; só a string do erro vaza em inglês. Quebra de i18n/consistência. (O mesmo extractWAError é usado em loadConfig/disconnect, então o vazamento se repete em outros erros 400 do módulo.)

## F-006 | categoria: visual | severidade: média | status: verificado
- Tela: cleanos/web/src/styles/tokens.css:90 (--clx-group-promocao: #8B5CF6) — evidência screenshot ss_2794n8h0q (lista) e ss_4513aeymc (editor)
- Passos: 1) Login admin → /painel/servicos 2) Observar qualquer serviço do grupo "Promoção" (ex.: Cleanox Completo - Promoção, Cleanox Premium - Promoção).
- Esperado: Chip do grupo "Promoção" em cor AMARELA/ÂMBAR conforme mockup de referência ("Promoção amarelo").
- Observado: Chip "Promoção" é renderizado em ROXO/VIOLETA (dark mode: #A78BFA, light mode: #8B5CF6). Diverge do mockup onde o chip aparece em tom amarelo/âmbar. A cor está definida em tokens.css como `--clx-group-promocao: #8B5CF6` (purple-500), sem qualquer variante amarela.
- (resolvedor) Correção: `--clx-group-promocao` e `*-bg` repintados na paleta âmbar alinhada a `--clx-warning` — light `#F59E0B` / `rgba(245,158,11,0.12)`, dark `#FBBF24` / `rgba(251,191,36,0.18)` (tokens.css:90,98,160,168). `.clx-chip-group-promocao` consome esses tokens (global.css:2189), então o chip fica amarelo nos dois temas. Validado: tsc -b exit 0, build ok, 198/198 testes verdes.
- (navegador) Re-teste em produção /painel/servicos (ss_3624u5p2m, zoom ss): chips "Promoção" (Cleanox Completo/Premium - Promoção) renderizam em ÂMBAR/dourado conforme esperado. VERIFICADO.

## F-007 | categoria: visual | severidade: baixa | status: verificado
- Tela: cleanos/web/src/styles/tokens.css:94 (--clx-group-colchao: #DB2777) — evidência screenshot ss_2794n8h0q (lista Colchão chip)
- Passos: 1) Login admin → /painel/servicos 2) Observar qualquer serviço do grupo "Colchão" (ex.: Colchão casal, Cama box casal).
- Esperado: Chip do grupo "Colchão" em cor LILÁS/LAVANDA, igual ao chip "Sofá" (--clx-group-sofa: #C084FC). Mockup mostra Sofá e Colchão com a mesma paleta roxa-clara ("Sofá/Colchão lilás").
- Observado: Chip "Colchão" é rosa/magenta (dark mode: #F472B6, light mode: #DB2777), enquanto "Sofá" é lilás (#D8B4FE/#C084FC). Os dois grupos deveriam ter a mesma cor conforme mockup, mas Colchão usa pink-500/pink-600 em vez de purple-300/purple-400.
- (resolvedor) Correção: `--clx-group-colchao` e `*-bg` igualados à paleta do Sofá — light `#C084FC` / `rgba(192,132,252,0.16)`, dark `#D8B4FE` / `rgba(216,180,254,0.18)` (tokens.css:94,102,164,172). `.clx-chip-group-colchao` agora fica visualmente idêntico ao chip Sofá nos dois temas. Validado: tsc -b exit 0, build ok, 198/198 testes verdes.
- (navegador) Re-teste em produção /painel/servicos (ss_3624u5p2m): chips "Colchão" (Cama box casal/solteiro, Colchão casal/king/queen/solteiro) renderizam em LILÁS/lavanda, idêntico aos chips "Sofá" (Sofá 2/3/4/5-6 lugares, Sofá retrátil). VERIFICADO.

## F-005 | categoria: usabilidade | severidade: média | status: corrigido
- Tela: painel → Usuários (cleanos/web/src/pages/painel/Usuarios.tsx, coluna E-MAIL) — evidência screenshot ss_5148rwgb8. Causa de dados: todos os users com `emailVisibility=false` (seed/criação).
- Passos: 1) Login admin → Usuários.
- Esperado: numa tela de GESTÃO de usuários, o admin vê o e-mail (login) de todos — é como identifica/gerencia cada conta.
- Observado: a coluna E-MAIL aparece preenchida APENAS para o próprio admin logado (Ana Admin). Para Gabriel Gerente, Lucas e Pedro Profissional a coluna fica VAZIA. Confirmado via API: GET /api/collections/users/records (como admin) retorna `email` só do próprio registro; os demais vêm SEM o campo email porque `emailVisibility=false` em todos. Logo o admin não consegue ver/identificar por e-mail os usuários que administra (e e-mail é a credencial de login). A regra/coluna existe mas fica inutilizável para 3 de 4 contas. Correção provável: setar `emailVisibility=true` ao criar usuários (e no seed), ou expor e-mail a admin no endpoint. Não é exposição indevida — é tela interna restrita a admin.
- (resolvedor) Correção: migration 1700000013_fix_email_visibility.js seta `emailVisibility=true` em todos os users existentes (idempotente); seed.js:49 e hook onRecordCreate de users já garantem `true` nas novas contas.

## F-501 | categoria: funcional | severidade: média | status: verificado
- Tela: cleanos/pb/pb_hooks/whatsapp_routes.pb.js:311 (buildRelatorioTexto, `const total = Math.max(0, valorPrincipal + valorAdicionais)`) × cleanos/web/src/pages/painel/OSExecucaoPage.tsx:158-159 (descontos é estado LOCAL) e :411/:433 (preview usa descontos) × ausência de coluna `descontos` no schema (confirmado: sem match em 1700000001_init_collections.js e em collections.ts)
- Passos: 1) Abrir a execução de uma OS REAL (/painel/ordens/:osId/execucao). 2) No "Resumo financeiro", digitar um valor em "− Descontos (R$)" (ex.: R$ 50). 3) Clicar "Finalizar e gerar relatório" — o modal mostra Total = principal + adicionais − 50 e a linha "Descontos −R$ 50,00". 4) Clicar "Enviar por WhatsApp" (OS real → dispara POST /api/cleanos/os/{id}/relatorio).
- Esperado: a mensagem que o cliente recebe reflete o MESMO total exibido no preview/que o operador aplicou — com o desconto abatido.
- Observado: o desconto NUNCA chega ao cliente. (a) `descontos` é estado puramente local da página (comentário em :158 "descontos não tem coluna no PB — permanece local"), nunca persistido — não há coluna nem no schema nem no tipo. (b) O backend que monta a mensagem REAL (buildRelatorioTexto) não lê desconto algum e calcula `total = valorPrincipal + valorAdicionais` (:311), sem subtrair desconto e sem linha "Descontos". Resultado: o operador aplica e visualiza um total com desconto, mas o cliente recebe por WhatsApp o total CHEIO (maior), divergindo do combinado/previsto. O recurso de desconto é, na prática, cosmético: evapora sem registro em PB, Financeiro ou relatório. NOTA correlata: como o servidor remonta a mensagem a partir do PB, QUALQUER edição de rotina (checklist/adicionais/observações) ainda dentro do debounce de 800ms (OSExecucaoPage.tsx:304-315) e não salva no instante do envio também sai divergente do preview. Correção: persistir `descontos` na OS e fazer buildRelatorioTexto subtrair desconto (espelhar calcTotalOS), ou remover o campo de desconto da UI até existir suporte fim-a-fim.
- (resolvedor) Correção: causa raiz = desconto não persistido, logo o backend o desconhecia. (1) Nova migration `1700000011_os_descontos.js` adiciona coluna `descontos` (NumberField >=0) em `ordens_servico` (idempotente + DOWN). (2) `OSExecucaoPage.tsx`: descontos passa a ser hidratado de `rec.descontos` e persistido junto do trio de rotina (debounce) e no save do snapshot; `serializeRoutine` inclui descontos; tipo `OSExecPatch` (osStore.ts) e `OrdemServico` (collections.ts) ganham `descontos?`. (3) `whatsapp_routes.pb.js` buildRelatorioTexto (:311) lê `os.get("descontos")`, faz `total = principal + adicionais − descontos` (espelha calcTotalOS) e imprime linha "Descontos: -R$ x". Commit 72d6295. Validado: `tsc -b` exit 0 + `npm run build` ok; migration aplicada sem erro num COPY descartável de pb_data (`migrate up` → "Applied 1700000011"); produção/PB real não tocados. Preview (calcTotalOS) e relatório do backend agora usam a mesma fórmula.
- (reviewer) Verificação: commit 72d6295 ok. Migration usa o id de coleção correto (`ordserv00000001`, init_collections.js:122), é idempotente (guard `if (!fields.getByName)`) e tem DOWN. buildRelatorioTexto (:323) calcula `total = max(0, principal + adicionais − descontos)`, idêntico a calcTotalOS (store.ts:329); o `valorAdicionais` do backend só soma `cobraveis` (aprovado/nao_requer, :305), espelhando os "cobráveis" do front — sem total negativo, com linha "Descontos: -R$". OSExecucaoPage hidrata `rec.descontos` (:217), inclui no serializeRoutine/debounce/persistSnapshot e nas deps do effect (:324). Preview e WhatsApp passam a bater. VERIFICADO.

## F-502 | categoria: funcional | severidade: baixa | status: verificado
- Tela: cleanos/pb/pb_hooks/whatsapp_routes.pb.js:178-182 (rota /a-caminho) × contraste com :420-423 da rota /relatorio (que já tem o guard)
- Passos: 1) Existir uma OS `em_andamento` cujo relacionamento `cliente` esteja vazio/órfão (ex.: cliente removido, import, manutenção SQL). 2) Profissional dono chama POST /api/cleanos/os/{id}/a-caminho.
- Esperado: erro de negócio explícito (400 "Esta OS não possui cliente associado."), igual ao que a rota /relatorio passou a fazer (guard `if (!clienteId)` em :421-423, citado como "F-03").
- Observado: a rota /a-caminho lê `clienteId = lib.relId(os.get("cliente"))` e chama direto `$app.findRecordById("clientes", clienteId)` SEM checar vazio (:179-180). Com clienteId = '' o findRecordById lança 404/500 cru — exatamente o caso que a rota /relatorio blindou. Assimetria defensiva: a mesma correção não foi replicada aqui. Alcance limitado (cliente é required na OS), mas a inconsistência é real e o profissional recebe um erro genérico de servidor em vez da mensagem amigável. Adicional: `uazapi.normalizePhone` devolve "" para telefone ausente/inválido (uazapi.js:104), e a /a-caminho seguiria para `sendText` com number vazio sem validação prévia.
- (resolvedor) Correção: replicado na /a-caminho (whatsapp_routes.pb.js:~179) o guard que a /relatorio já tinha — `if (!clienteId) throw new BadRequestError("Esta OS não possui cliente associado.")` ANTES do findRecordById. Também adicionado o guard do "Adicional": `if (!numero) throw new BadRequestError(...)` após `normalizePhone`, evitando sendText com número vazio. Commit dbfffe8. Validado: `node --check` syntax OK; lógica espelha a rota /relatorio (:421-423) já testada. Mesmo arquivo do F-501, commit separado.
- (reviewer) Verificação: commit dbfffe8 ok. Guard `if (!clienteId) throw new BadRequestError(...)` em :183, ANTES do findRecordById, espelha exatamente o de /relatorio (:435); guard de telefone `if (!numero)` em :192 após normalizePhone, evitando sendText com número vazio. BadRequestError é o mesmo tipo usado em todo o arquivo (:73,:144,:396,:435). Retorna 400 amigável em vez de 404/500 cru. VERIFICADO.

## F-503 | categoria: funcional | severidade: baixa | status: verificado
- Tela: cleanos/web/src/components/os/EvidenciasSection.tsx:257-262 (`.then` troca tmp pelo registro real) × :312-316 (handleLegenda retorna cedo para itens tmp)
- Passos: 1) Na execução da OS, adicionar uma foto (upload começa, item otimista com legenda = nome do arquivo). 2) ENQUANTO o upload está em curso (overlay "Enviando…"), editar o campo Legenda da foto.
- Esperado: a legenda digitada durante o upload é preservada e persistida.
- Observado: a edição é perdida silenciosamente. handleLegenda atualiza o estado local mas, por ser id temporário (`isTmp`), retorna em :316 sem agendar persistência ("ainda subindo — persiste depois via vínculo/recarregar"). Quando o upload conclui, o `.then` substitui o item tmp inteiro por `real` (`onChange((prev) => prev.map((f) => (f.id === tmpId ? real : f)))`, :259), e `real` carrega a legenda ORIGINAL (nome do arquivo enviado no createEvidencia), sobrescrevendo o que o usuário digitou. A legenda editada nunca vai ao PB nem permanece na UI após o swap. Janela = duração do upload; perda de dado de entrada do usuário sem aviso.
- (resolvedor) Correção: causa raiz = `handleLegenda` (:312) atualiza só o estado local e retorna cedo para id tmp, e o `.then` do upload (:257) substituía o item tmp inteiro pelo `real` (com a legenda original do nome do arquivo). Adicionado ref `editedTmpLegenda` (EvidenciasSection.tsx:~152): em `handleLegenda`, quando `isTmp`, a edição é registrada no ref antes do return. No `.then`, se houver edição que difere de `real.legenda`, o item é MESCLADO (`{...real, legenda: edited}`) em vez de sobrescrito, e a legenda é persistida no PB via `updateEvidencia(real.id, {legenda})`; o ref é limpo no `.then` e no `.catch`. Commit a32675e. Validado: `tsc -b` exit 0, `vitest run` 287/287 verdes, `npm run build` ok. Solução via ref (não estado) evita problemas de timing do setState assíncrono no momento do swap.
- (reviewer) Verificação: commit a32675e ok. `editedTmpLegenda` (ref keyed por tmpId, :152) registra a edição no caminho tmp do handleLegenda (:334-336) antes do return; no `.then` o item é MESCLADO (`{...real, legenda: edited}`, :265) e persistido via updateEvidencia (:274) só quando `hasEdit`. Ref é deletado tanto no `.then` (:264) quanto no `.catch` (:283) → sem leak entre uploads (chave única por upload). Edge legenda vazia: `edited || undefined` local + PB recebe '' (ambos = "sem legenda", ok). Helpers updateEvidencia/describeOSError/notifyError em escopo. Edição durante upload preservada e persistida. VERIFICADO.

## F-008 | categoria: visual | severidade: média | status: verificado
- Tela: ss_7010fukhb (produção /painel/servicos, lista filtrada com 1 resultado) · zoom ss_3286sy85h-anterior
- Passos: 1) Login admin → /painel/servicos. 2) Filtrar/buscar a ponto de a tabela ficar com poucas linhas (ex.: buscar "QA TESTE" → 1 linha). 3) Clicar no kebab (⋮ "Mais ações") da última/única linha. 4) Observar o menu Duplicar/Excluir.
- Esperado: o menu suspenso Duplicar/Excluir aparece inteiro e clicável abaixo (ou acima) do botão.
- Observado: o menu abre mas fica CLIPADO pelo container da tabela/card (overflow) — só a borda superior arredondada do menu aparece logo abaixo da linha; os itens "Duplicar"/"Excluir" ficam invisíveis ao usuário. Confirmado que os itens EXISTEM no DOM (refs menuitem "Duplicar"/"Excluir", acionáveis programaticamente) — é puramente recorte visual. Acontece quando a linha está perto da borda inferior do container curto (poucas linhas). Com a lista cheia o menu cai dentro da área longa e fica visível, mascarando o bug. Resultado: com a lista filtrada, Duplicar/Excluir ficam inacessíveis via mouse. Provável correção: portal/menu flutuante (position fixed) ou permitir overflow visível no container quando o menu está aberto.
- (resolvedor) Correção: causa raiz = `.svc-kebab-menu` (position:absolute, global.css:3077) é clipado pelos ancestrais com overflow recortante — `.table-wrap{overflow:hidden}` (:811) e `.table-scroll{overflow-x:auto}` (:818, que computa overflow-y para auto, recortando vertical). Como CSS não permite overflow-x:auto + overflow-y:visible juntos, optei pela 2ª via sugerida (overflow visível enquanto o menu está aberto): adicionadas regras `.table-wrap:has(.svc-kebab-menu){overflow:visible}` e `.table-wrap:has(.svc-kebab-menu) .table-scroll{overflow:visible}` (global.css:~3113). O `.svc-kebab-menu` só existe no DOM quando aberto (ServicosListPage.tsx:354 render condicional), então o `:has()` ativa apenas nesse intervalo transiente — fora disso o scroll/clip normais ficam intactos. Commit 849264f. Validado: `npm run build` exit 0 (CSS empacotado, 82.00→82.11 kB). Fix CSS-only, sem JS/portal. (`:has()` suportado em todos navegadores atuais; app é ferramenta interna.)
- (reviewer) Verificação: commit 849264f — a correção do kebab está CORRETA: `.table-wrap:has(.svc-kebab-menu){overflow:visible}` + `... .table-scroll{overflow:visible}` (global.css:3119-3120). O `.svc-kebab-menu` só existe no DOM quando aberto (render condicional), então o `:has()` ativa só nesse intervalo; com menu fechado o `.table-wrap{overflow:hidden}`/`.table-scroll{overflow-x:auto}` normais ficam intactos — sem regressão de scroll. VERIFICADO para o fix. ⚠️ CONTAMINAÇÃO DE COMMIT (não bloqueia o finding, mas exige limpeza): o commit 849264f adiciona 803 linhas em global.css, das quais ~794 são CSS do MÓDULO FINANCEIRO em andamento (`.fin-*`, varrido do working tree não-commitado) — só ~9 linhas são o fix do kebab. As regras extras são todas `.fin-*`-namespaced (verificado: nenhum override de seletor global; só 3 @media internos), então NÃO regridem estilos existentes, mas o commit deixou de ser cirúrgico e carrega trabalho inacabado de outra frente (cujos .tsx sequer estão na branch). RESOLVEDOR: re-isolar o commit do F-008 para conter apenas as 2 regras `:has()` (unstage do bloco Financeiro) antes do merge.
- (resolvedor) Re-isolado: commit b0316e6 removeu o CSS .fin-* contaminante (794 linhas do módulo Financeiro varridas por engano); árvore final só contém o fix do kebab (`.table-wrap:has(.svc-kebab-menu)` + `.table-scroll`); `.fin-grid` pré-existente preservado; build ok (tsc -b + vite build exit 0). Diff líquido vs 849264f^ = apenas as 9 linhas do fix.

## F-009 | categoria: visual | severidade: baixa | status: verificado
- Tela: ss_45815p4ge (produção /painel/servicos/novo) — editor, card Informações principais, campo Valor base
- Passos: 1) Login admin → Novo serviço. 2) Tipo de valor = Faixa. 3) Preencher mín > máx (ex.: mín R$1,00, máx R$0,50) → aparece o erro "O valor máximo deve ser maior que o mínimo." 4) Trocar Tipo de valor para Variável (que não usa mín/máx). 5) Salvar.
- Esperado: ao mudar para Variável o erro de faixa some imediatamente (campo mín/máx deixa de existir; a validação não se aplica mais).
- Observado: ao trocar para Variável o campo Valor base colapsa para um único campo, mas a mensagem de erro vermelha "O valor máximo deve ser maior que o mínimo." PERMANECE renderizada sob o campo. O save procede normalmente (Variável libera a validação e o serviço é criado), então é mensagem de erro órfã/stale — confunde o operador (parece que há erro mas o save funciona). Baixo impacto (cosmético, não bloqueia). Provável correção: limpar o estado de erro de faixa ao trocar o Tipo de valor.
- (resolvedor) Correção: causa raiz = `setField` (ServicoEditorPage.tsx:187) só removia `fieldErrs[key]` da PRÓPRIA chave alterada; o erro de faixa é gravado sob `valorBaseMaxCents` (:228), então trocar `tipoValor`→Variável/Fixo (key `tipoValor`) não tocava o erro órfão. `setField` agora também apaga `valorBaseMaxCents` quando `key==='tipoValor' && value!=='faixa'` (a validação mín/máx só existe em faixa). Commit 4359331. Validado: `tsc -b` exit 0. Fix mínimo de 1 cláusula no clear de erros.
- (reviewer) Verificação: commit 4359331 ok. `dropMax = key==='tipoValor' && value!=='faixa' && !!prev.valorBaseMaxCents` (ServicoEditorPage.tsx) limpa o erro órfão só ao SAIR de faixa; ao VOLTAR para faixa (`value==='faixa'`) dropMax=false, nada é suprimido e a validação mín/máx volta a rodar normalmente no save/blur — sem mascarar validação legítima. Demais chaves seguem o comportamento original. VERIFICADO.

## F-010 | categoria: funcional | severidade: média | status: verificado
- Tela: ss_1821bfvxh (produção /painel/ordens/:id/execucao logo após criar a OS) vs ss_9995345mt (após re-selecionar o serviço no dropdown da execução)
- Passos: 1) Login admin → Ordens → Nova OS. 2) Escolher um cliente e um SERVIÇO que possua checklist padrão cadastrado (ex.: QA TESTE Servico com itens "Item B/Item A"), atribuir profissional, salvar. 3) Abrir a Execução dessa OS (botão →). 4) Observar o card "Checklist de execução".
- Esperado: como a OS já tem serviço principal (snapshot capturado na criação), o checklist de execução deveria vir populado com os itens do checklist padrão do serviço (X de N), pronto para o profissional marcar.
- Observado: o snapshot do serviço É capturado na criação (card "Serviço principal" mostra a cópia com valor/observação e a nota "Capturado em ..."), MAS o checklist de execução fica VAZIO — "Checklist vazio · 0 de 0 concluídos · Selecione o serviço principal para gerar o checklist de execução." O checklist só é gerado quando alguém RE-SELECIONA o serviço no dropdown da própria tela de execução (aí aparece "0 de 2", Item B/Item A, e toast "Snapshot capturado"). Ou seja: uma OS criada pelo modal Nova OS com serviço definido chega na execução SEM checklist; o profissional não vê os itens a cumprir até refazer a seleção do serviço. Gap entre o snapshot do create (modal) e a geração do checklist (execução). Provável correção: gerar o checklist de execução a partir do checklist padrão do serviço no momento da criação/atribuição da OS (espelhar o que o re-select faz).
- (resolvedor) Correção: causa raiz = o modal "Nova OS" cria a OS enviando só o relation `servico` (OrdensServico.tsx:210/227, sem snapshot/checklist); o hook `fillServiceSnapshot` (os_logic.js) congelava o snapshot mas NÃO materializava `checklist_exec`. Adicionado, no mesmo hook (logo após `record.set("service_snapshot")`), a derivação de `checklist_exec` a partir de `checklistPadrao` (ordenado por `ordem`, ids `cke_*`, status `pendente`) — espelha `snapshotToChecklistExec` do frontend. Só preenche quando `checklist_exec` está vazio (guarda anti-clobber) e o bloco só é alcançado quando o snapshot acabou de ser congelado (a UI, que envia snapshot+checklist juntos, retorna antes na guarda de imutabilidade). Commit e99c8cc. Validado em RUNTIME contra COPY descartável do pb_data (PB serve :8092 com os hooks reais): POST de OS no formato do modal (cliente+servico, sem checklist) retornou `checklist_exec` com 6 itens, todos `pendente`, ids únicos; PATCH posterior (status/observacoes/descontos) preservou os 6 itens (sem clobber). Produção não tocada.
- (reviewer) Verificação: commit e99c8cc ok. O bloco roda dentro de `fillServiceSnapshot` LOGO após `record.set("service_snapshot")`, e só é alcançado quando o snapshot acabou de ser congelado — a guarda de imutabilidade (`if (existing && existing.serviceId) return`, os_logic.js:138) faz a UI (que envia snapshot+checklist juntos) retornar antes, então não clobbera o caminho de re-seleção. Anti-clobber adicional: só preenche quando `checklist_exec` está vazio (:188). Itens derivados de `checklistPadrao` ordenado por `ordem`, `status:"pendente"`, shape `{id,titulo,status}` idêntico a snapshotToChecklistExec do front (store.ts:301); ids únicos (Date.now+índice+random). Runtime testado pelo resolver (6 itens, PATCH preservou). VERIFICADO.

## F-011 | categoria: funcional | severidade: média | status: verificado
- Tela: cleanos/web/src/lib/os/osStore.ts:143-151 (listEvidencias) — sintoma na Execução da OS, seção Evidências
- Passos: 1) Abrir a Execução de uma OS que tenha evidências (fotos). 2) Observar a seção Evidências / rede.
- Esperado: as fotos de evidência da OS são listadas e reexibidas.
- Observado: listEvidencias usa getFullList({filter:'os = {:osId}', filterParams:{osId}}), mas o SDK JS do PocketBase NÃO tem a opção filterParams → o placeholder {:osId} vai cru no filter e o servidor retorna 400 (invalid filter) em TODA carga. Erro engolido (sem console); mascarado por empty-state em OS sem fotos, mas qualquer OS com evidências nunca reexibe suas fotos. Reproduzido 2x em produção (GET os_evidencias 400). Correção: filter: pb.filter('os = {:osId}', {osId}).
- (navegador) Confirmado em produção na varredura do Painel (2x GET os_evidencias 400).
- (resolvedor) Correção: listEvidencias agora usa filter: pb.filter('os = {:osId}', {osId}) — binding server-side seguro; removida a opção inexistente filterParams (único uso no arquivo). Valida com tsc -b (exit 0). Commit F-011.
- (reviewer) Verificação: commit 2a7f5f5 ok. osStore.ts:147 usa `filter: pb.filter('os = {:osId}', { osId })` — `pb.filter()` é o helper OFICIAL do SDK JS que interpola/escapa o binding server-side (imune a injeção via osId), substituindo corretamente a opção inexistente `filterParams`. Grep em cleanos/web/src: ZERO ocorrências residuais de `filterParams` (era o único uso). Query agora é aceita pelo servidor → OS com evidências reexibe as fotos. VERIFICADO.

## F-012 | categoria: funcional | severidade: baixa | status: verificado
- Tela: cleanos/web/src/pages/painel/financeiro/lancamentos/LancamentoFormModal.tsx (default do campo DATA) + lancamentos/dates.ts
- Passos: 1) Após 21h BRT, abrir Financeiro → Lançamentos → Novo lançamento. 2) Observar o campo Data.
- Esperado: o campo Data vem com o dia LOCAL corrente (o mesmo que o sistema exibe).
- Observado: o default usa calendário UTC (new Date().toISOString()); após 21h BRT o campo já nasce com o dia SEGUINTE (ex.: sistema mostra 30/06 e o form pré-preenche 01/07). Mesma classe de fuso de F-222/F-203/F-204, superfície NOVA (default do form). Correlato: a aba "Contas a pagar/receber" abre em Julho enquanto abas irmãs abrem em Junho (mesma raiz UTC). Evidência ss_4790ykqgl.
- (navegador) Confirmado em produção na varredura do Financeiro.
- (resolvedor) Correção: novo helper `todayLocalInput()` em lancamentos/dates.ts deriva 'YYYY-MM-DD' pelos getters LOCAIS (getFullYear/getMonth/getDate) em vez de `toISOString()` (UTC); `todayInput()` em LancamentoFormModal.tsx passou a usá-lo, então o default do campo Data reflete o dia local BRT. Validado com `tsc -b` (sem erros). A nota correlata da aba "Contas a pagar/receber" (mês errado) tem raiz na inicialização do período em outro arquivo (fora do escopo destes 2 arquivos) — não alterada aqui.
- (reviewer) Verificação: commit 7f063cc ok. Novo `todayLocalInput()` (dates.ts:56) monta 'YYYY-MM-DD' pelos getters LOCAIS (getFullYear/getMonth+1/getDate com padStart) — reflete o dia BRT corrente, não o dia UTC de `toISOString()`. `todayInput()` (LancamentoFormModal.tsx:73) passa a chamá-lo; formato de saída idêntico ao esperado pelo `<input type="date">`, então não regride o parse. É a ÚNICA mudança no modal — nenhum outro uso de data (toDateInputValue nos demais campos) foi tocado. A nota da aba "Contas a pagar/receber" foi corretamente deixada fora de escopo (raiz em outro arquivo). VERIFICADO.

## F-220 | categoria: funcional | severidade: média | status: verificado
- Tela: cleanos/web/src/lib/financeiro/store.ts:214-219 (ajustarSaldoConta); ContasCarteiras.tsx:190-196 (handleTransfer), :124-131 (editar conta); cleanos/pb/pb_hooks/os_financeiro_lib.js:141-143
- Passos: 1) Hook OS→Financeiro incrementa saldo_atool de uma conta (receita de OS) ao mesmo tempo em que o painel grava saldo_atual ABSOLUTO (transferência/editar conta) a partir de uma leitura stale.
- Esperado: incrementos concorrentes se conservam.
- Observado: as escritas absolutas do painel (handleTransfer / editar conta) partem de um saldo lido antes e podem sobrescrever o incremento concorrente do hook OS→Financeiro (lost-update). O hook usa incremento server-side; o painel usa set absoluto. Classe: integridade / saldo dessincronizado.
- (navegador) Verificação parcial em produção: no HAPPY-PATH a transferência conserva a soma (R$10 Banco Inter→Caixa físico manteve Saldo geral R$9.126,26; revertido). A CORRIDA lost-update NÃO foi reproduzível com segurança (exigiria concluir OS concorrente, mutação não reversível) — risco permanece por inspeção de código.
- (resolvedor) Correção: painel deixou de gravar `saldoAtual` ABSOLUTO a partir de estado de UI stale. `ajustarSaldoConta` (store.ts) foi exportada (relê o saldo fresco do servidor IMEDIATAMENTE antes de somar o delta — mesma estratégia incremental do hook OS→Financeiro) e adicionada `transferirSaldo(fromId,toId,valor)` que aplica −valor/+valor via deltas frescos com rollback. ContasCarteiras.tsx: (a) editar conta atualiza nome/tipo/saldoInicial/ativo SEM saldoAtual e aplica só o delta (`saldoInicial - editing.saldoInicial`) via `ajustarSaldoConta`; (b) transferência usa `transferirSaldo`. Assim um incremento concorrente do hook é relido (não clobbereado) — a janela de lost-update cai de "carga-da-página→ação" para milissegundos dentro da chamada (PB 0.39.4 REST não tem incremento atômico SQL; a atomicidade forte do lado servidor é escopo de F-221). Commit F-220. Validado: `tsc -b` exit 0. Arquivos: cleanos/web/src/lib/financeiro/store.ts, cleanos/web/src/pages/painel/financeiro/ContasCarteiras.tsx.
- (reviewer) Verificação (VERIFICADO COM RESSALVA): commit 83501e3 ok e é uma melhoria real, mas NÃO fecha 100% a corrida — é MITIGAÇÃO PARCIAL aceitável. Detalhe: `ajustarSaldoConta` (store.ts:222, agora exportada) faz read-modify-write em DUAS chamadas REST separadas (getConta → updateConta) sem lock otimista/incremento atômico SQL (o PB 0.39.4 REST não oferece isso). Se o hook OS→Financeiro incrementar EXATAMENTE na janela entre o getConta e o updateConta, o lost-update AINDA ocorre — a janela porém cai de "carga-da-página→ação do usuário" (segundos/minutos) para "milissegundos dentro da chamada". O painel deixou de gravar saldoAtual ABSOLUTO a partir de estado de UI stale: editar-conta (ContasCarteiras.tsx:137) aplica só o delta `saldoInicial−editing.saldoInicial` via ajustarSaldoConta e NÃO envia saldoAtual no updateConta; transferência usa `transferirSaldo` (:196) com deltas −/+ e rollback do débito se o crédito falhar. Corrida residual é intrínseca à ausência de incremento atômico no PB REST — a atomicidade forte server-side pertence ao hook (F-221) e não cruza a fronteira frontend↔hook. RESSALVA registrada; aceitável como o teto do que a plataforma permite sem transação server-side no caminho do painel. VERIFICADO com ressalva.
- (resolvedor) Rodada 2 — endurecimento: a ressalva foi FECHADA movendo o ajuste de saldo do painel para o SERVIDOR, transacional. Novo arquivo `cleanos/pb/pb_hooks/fin_routes.pb.js` com duas rotas — `POST /api/cleanos/contas/ajustar {contaId,delta}` e `POST /api/cleanos/contas/transferir {de,para,valor}` — ambas `$apis.requireAuth()` + `requireAdminOrGerente` (o mesmo escopo COFRE_FIN admin/gerente das collection rules de fin_* na migration 14) e ambas dentro de `$app.runInTransaction` (mesmo padrão atômico do hook F-221): leem o saldo_atual FRESCO e aplicam o delta na MESMA transação DB — não existe mais janela entre leitura e escrita, então nenhum incremento concorrente do hook OS→Financeiro é clobbereado. store.ts: `ajustarSaldoConta`/`transferirSaldo` agora chamam essas rotas via `pb.send` (JSON) em vez de fazer read-modify-write em 2 chamadas REST ou gravar saldo_atual absoluto no cliente; best-effort 404 preservado em ajustarSaldoConta (conta órfã → no-op). ContasCarteiras.tsx não precisou mudar (editar-conta e transferência já delegavam a esses dois helpers). Commit F-220 (rodada 2). Validado: `node --check` fin_routes.pb.js OK; `tsc -b` exit 0; `vitest run` store 69/69 (mock `pb.send` espelha as rotas + novos testes de ajustar/transferir/conservação de saldo). RUNTIME em CÓPIA DESCARTÁVEL do pb_data (PB 0.39.4, :8095, SEM envs de webhook): transferência Inter→Caixa −100/+100 atômica; ajuste incremental +50; **40 ajustes +10 CONCORRENTES em Nubank 3250→3650 sem NENHUM incremento perdido**; 30 transfers + 30 ajustes concorrentes na MESMA conta conservaram a soma exata (Inter voltou a 6350, Nubank +30 = 3680) — prova de que o lost-update residual foi eliminado; guard: profissional 403, sem-auth 401, valor≤0 → 400. Arquivos: cleanos/pb/pb_hooks/fin_routes.pb.js (novo), cleanos/web/src/lib/financeiro/store.ts, cleanos/web/src/lib/financeiro/store.test.ts. Mantido status `verificado` — reviewer re-verifica o novo commit.
- (reviewer) Rodada 2 — verificação: CONFIRMADO `verificado`. Li fin_routes.pb.js (commit 78ca257): (1) ambas as rotas leem o saldo FRESCO (`findRecordById` DENTRO do `$app.runInTransaction`) e aplicam o delta no MESMO txApp com `txApp.save` — não há janela read-modify-write fora da transação. (2) Guard OK nas duas rotas: `$apis.requireAuth()` (posicional) + `h.requireAdminOrGerente(e)` no topo do handler (whatsapp_helpers: sem auth → UnauthorizedError/401; role≠admin/gerente → ForbiddenError/403). (3) Frontend não grava mais saldo_atual absoluto no fluxo de mutação: editar-conta faz `updateConta` só com nome/tipo/saldoInicial/ativo + `ajustarSaldoConta(delta)`; único write absoluto restante é `saldoAtual: saldoInicial` no CREATE de conta nova (sem concorrência — aceitável). Nenhum outro grep de saldoAtual em store.ts/ContasCarteiras.tsx grava absoluto pós-criação. (4) INTERAÇÃO hook×rota: os_financeiro_lib.js (F-221) incrementa `saldo_atual` fresco dentro do seu próprio `runInTransaction`; a rota /ajustar faz o mesmo padrão fresh-read-in-tx. Como o SQLite do PB serializa escritas, os dois caminhos CONSERVAM (cada incremento é relido) e NÃO se sobrepõem nem duplicam — mesma estratégia atômica já aceita em F-221. (5) /transferir aplica −valor/+valor no mesmo txApp (all-or-nothing; se um save falha, a tx inteira reverte); `valor<=0`→400, `de===para`→400. (6) 404/best-effort: `ajustarSaldoConta` engole 404 (no-op) → não quebra; `transferirSaldo`/`definirContaPadrao` propagam, mas os handlers de UI têm try/catch → banner de erro, sem crash. RESSALVA (não bloqueia): saldo negativo em /transferir é PERMITIDO (sem floor) — consistente com o painel atual e com cartão negativo; aceitável, mas se o negócio quiser trava de overdraft é um guard futuro. RESSALVA menor: o botão/rota são protegidos server-side (403), mas a UI não esconde o botão de não-admin — o painel financeiro já é cofre COFRE_FIN no roteamento, então é só cosmético. `tsc -b` exit 0 e `vitest run` 314/314 reconferidos localmente. Sem regressão.

## F-221 | categoria: funcional | severidade: média | status: verificado
- Tela: cleanos/pb/pb_hooks/os_financeiro_lib.js:135-147 (save lanc vs save conta), :42-49 (anti-dup); cleanos/web/src/lib/financeiro/store.ts:278 (deleteLancamento)
- Passos: 1) Concluir OS: o hook salva o lançamento, depois ajusta o saldo. 2) Se o ajuste de saldo falhar após o lançamento persistir.
- Esperado: atomicidade (lançamento + saldo) ou retry seguro.
- Observado: lançamento é persistido mas o ajuste de saldo pode ser engolido (falha parcial); o anti-dup (:42-49) então impede reaplicar o ajuste em retry → saldo fica defasado. Delete posterior do lançamento pode deixar saldo negativo/defasado. Classe: erro engolido / falha parcial / integridade.
- (navegador) Verificação em produção do HAPPY-PATH: idempotência OK (re-salvar a OS concluída 3x NÃO criou 2º lançamento nem somou saldo de novo); delete do lançamento via_os reverteu o saldo com precisão ao baseline (sem negativo). O cenário patológico (falha parcial no save do saldo) NÃO foi reproduzível nesta sessão — risco permanece por inspeção de código.
- (resolvedor) Correção: o save do lançamento e o ajuste de `saldo_atual` da conta passaram a rodar na MESMA transação via `app.runInTransaction(txApp => { txApp.save(lanc); ...saldo... })` (os_financeiro_lib.js). Antes, `app.save(lanc)` persistia e o ajuste de saldo vinha depois num try/catch que ENGOLIA a falha → lançamento sem saldo (defasagem), e o anti-dup (:42-49) bloqueava o retry. Agora: ou os DOIS saves persistem, ou NENHUM (rollback) — numa falha o lançamento é revertido junto, então o anti-dup não trava um retry legítimo e o saldo nunca fica defasado. Best-effort preservado no nível do handler (os_financeiro.pb.js envolve em try/catch e sempre chama e.next() → exceção aqui não bloqueia a conclusão da OS). O DELETE (store.ts:278) já revertia o saldo com PRECISÃO via `-efeitoNoSaldo(tipo,valor,status)` do próprio lançamento e, com o helper `ajustarSaldoConta` agora incremental (F-220), o revert relê o saldo fresco sem clobber — sem mudança necessária. Commit F-221. Validado: `node --check` OK; RUNTIME em CÓPIA descartável do pb_data (PB 0.39.4, porta 8091, migrations limpas sem os .d.ts/.db, hooks reais, SEM envs de webhook): OS create-as-concluida (valor_pago=150, pix) → 1 lançamento receita via_os na 1ª conta ativa (Banco Inter) + saldo 6450→6600 atômico; 2× PATCH na OS → continua 1 lançamento e saldo 6600 (anti-dup + sem re-incremento). Arquivo: cleanos/pb/pb_hooks/os_financeiro_lib.js.
- (reviewer) Verificação: commit 358e3cf ok. `app.runInTransaction((txApp) => {...})` (os_financeiro_lib.js:161) É a API correta e disponível no JSVM do PocketBase 0.39.4. AMBOS os saves estão DENTRO da transação e usam o handle transacional `txApp` (não o `app` externo): `txApp.save(lanc)` e, após reler a conta com `txApp.findRecordById("fin_contas", contaId)`, `txApp.save(conta)`. Semântica all-or-nothing: se o ajuste de saldo lançar, a transação faz rollback do lançamento junto → nunca fica lançamento sem saldo (nem vice-versa), e o anti-dup (:42-49) não trava um retry legítimo (o lançamento revertido não existe para bloquear). Caminho de erro: a exceção propaga para o caller (os_financeiro.pb.js) que a envolve em try/catch e sempre chama e.next() → best-effort preservado, conclusão da OS não é bloqueada. O log de sucesso ficou corretamente FORA da transação (só imprime se commit ok). VERIFICADO.

## F-222 | categoria: funcional | severidade: média | status: verificado
- Tela: cleanos/pb/pb_hooks/os_financeiro_lib.js:116 e :125; cleanos/web/src/lib/financeiro/store.ts:479-481 (dentroDoPeriodo/dateOnly)
- Passos: 1) Concluir uma OS às 21:00–23:59 BRT. 2) Abrir Relatórios/Visão geral do mês.
- Esperado: o lançamento via_os conta no dia/mês LOCAL (BRT) da conclusão.
- Observado: a data do lançamento via_os usa new Date() UTC puro; conclusões 21:00–23:59 BRT caem no dia/mês SEGUINTE nos relatórios. Mesma classe de F-203/F-204, em código NOVO do hook (não é duplicata). Correção: aplicar offset BRT ao gerar a data do lançamento.
- (resolvedor) Correção: a `data` do lançamento passou a ser gerada em BRT (UTC-3) — `new Date(Date.now() - 3h).toISOString()` em vez de `new Date().toISOString()` puro (os_financeiro_lib.js ~:116). Como os relatórios/Visão geral bucketizam pela PARTE-DATA 'YYYY-MM-DD' (dentroDoPeriodo/dateOnly, agruparPorData), subtrair 3h faz a parte-data refletir o dia BRT da conclusão; conclusões 21:00–23:59 BRT deixam de vazar para o dia/mês seguinte. Offset aplicado EXPLICITAMENTE (não via TZ local do processo, que na VPS pode ser UTC), espelhando F-203/F-204/getUtcDayBounds. Commit F-222. Validado: `node --check` OK; unit (node) — 22:30 BRT de 30/06 (=01:30Z 01/07) → parte-data 2026-06-30 (correto); RUNTIME na cópia descartável (PB 0.39.4, :8091): OS concluída gravou `data`=01:02 BRT com UTC=04:02 (offset −3h confirmado), parte-data = dia BRT. Arquivo: cleanos/pb/pb_hooks/os_financeiro_lib.js.
- (reviewer) Verificação: commit 9bdb90a ok. `BRT_OFFSET_MS = 3*60*60*1000` e `new Date(Date.now() - BRT_OFFSET_MS).toISOString()` (os_financeiro_lib.js:116,123) — subtrai 3h do instante UTC antes de serializar, então a PARTE-DATA 'YYYY-MM-DD' (que os relatórios usam via slice/dentroDoPeriodo/dateOnly, store.ts:479) reflete o dia BRT da conclusão; conclusões 21:00–23:59 BRT deixam de vazar para o dia/mês seguinte. Offset EXPLÍCITO (não via TZ do processo, que na VPS pode ser UTC) — coerente com F-203/F-204/getUtcDayBounds. Sobre o −3h fixo ignorar DST: RACIOCÍNIO CONFIRMADO — o Brasil aboliu o horário de verão em 2019 (Decreto 9.772/2019), então UTC−3 é constante o ano todo e o offset fixo é correto (só precisaria de revisão se o DST voltar). Nota: a string carrega o wall-time BRT rotulado "Z", consistente com o uso interno por parte-data nos relatórios financeiros. VERIFICADO.

## F-223 | categoria: funcional | severidade: baixa | status: verificado
- Tela: cleanos/pb/pb_hooks/os_financeiro_lib.js:93-102
- Passos: 1) Concluir uma OS com pagamento. 2) Verificar em qual conta a receita foi creditada.
- Esperado: a conta de destino da receita da OS deveria ser configurável/explícita.
- Observado: a receita de OS é SEMPRE creditada na "primeira conta ativa por nome asc"; criar uma 2ª conta cujo nome ordene antes redireciona TODA a receita silenciosamente, sem configuração. Classe: atribuição arbitrária de receita/saldo.
- (navegador) CONFIRMADO empírico em produção: a receita da OS de teste (R$200) caiu na "Banco Inter", que é a 1ª fin_conta ativa por nome asc.
- (resolvedor) Correção: destino tornado DETERMINÍSTICO/intencional via flag `padrao`. (1) Nova migration `1700000016_conta_padrao.js` adiciona `BoolField padrao` em fin_contas e — se nenhuma conta já for padrão — marca a 1ª ativa por nome asc (exatamente a que o hook escolhia antes) como `padrao=true`, PINANDO o destino atual de forma explícita e estável (idempotente + DOWN). (2) Hook `os_financeiro_lib.js` passou a escolher `ativo = true && padrao = true` (order nome, limit 1) com FALLBACK à 1ª ativa quando nenhuma marcada (ou campo ausente) — assim uma conta nova de nome anterior NÃO sequestra mais a receita. DECISÃO DE ESCOPO (registrada): o admin reatribui o padrão marcando outra conta (via Admin UI/PB por ora); um TOGGLE operador-facing em ContasCarteiras foi DEFERIDO porque exige alterar tipos fora do meu cluster (Conta/FinContaPB em types.ts/collections.ts, tocados por outros workers) — não bloqueia; o silent-redirect já está fechado. Commit F-223. Validado: `node --check` OK (hook+migration); RUNTIME cópia descartável (PB 0.39.4, :8092, migration 16 aplicada): campo criado + "Banco Inter" marcada padrao=true; criei "AAA Conta Nova" (ordena antes) e concluí OS R$200 → lançamento caiu em fininterbank001 (padrão, +200), AAA permaneceu R$0. Arquivos: cleanos/pb/pb_migrations/1700000016_conta_padrao.js, cleanos/pb/pb_hooks/os_financeiro_lib.js.
- (reviewer) Verificação: commit d3f307e ok. Migration 16: usa o collectionId CORRETO `fincontas000001` (definido em 1700000014_financeiro_schema.js:22). IDEMPOTENTE em duas camadas — (a) só adiciona `BoolField padrao` se `!contas.fields.getByName("padrao")`; (b) só pina um padrão se `findFirstRecordByFilter("fin_contas","padrao = true")` não achar nenhum (re-run após já pinado é no-op). Pina a 1ª ativa por nome asc — exatamente a que o hook escolhia antes, então NÃO redireciona receita existente. DOWN presente e simétrico: remove o campo via `removeById` com guard. Hook (os_financeiro_lib.js:98) escolhe `ativo = true && padrao = true` (order nome, limit 1) e cai no FALLBACK da 1ª ativa quando o campo está ausente (migration pendente) ou nenhuma marcada — determinístico, imune a conta nova de nome anterior. Sobre o TOGGLE operador-facing DEFERIDO: ACEITÁVEL como verificado — o bug reportado (silent-redirect da receita) está fechado; a reatribuição via Admin UI/PB é workaround suficiente para um finding de severidade BAIXA. NOTA: seria bom um toggle em ContasCarteiras numa rodada futura (registrado pelo resolvedor como decisão de escopo, dep. de tipos fora do cluster). VERIFICADO.
- (resolvedor) Rodada 2 — endurecimento: o TOGGLE operador-facing que estava DEFERIDO foi ENTREGUE. (1) Flag `padrao` exposta nos tipos: `Conta.padrao?` (types.ts) e `FinContaPB.padrao?` (collections.ts); pbToConta mapeia `padrao: rec.padrao ?? false` e contaToPB propaga. (2) Nova rota `POST /api/cleanos/contas/padrao {contaId}` em fin_routes.pb.js — `$apis.requireAuth()` + `requireAdminOrGerente` — que dentro de `$app.runInTransaction` DESMARCA todas as outras `padrao=true` e marca só a alvo na MESMA transação, garantindo o invariante "no máximo UMA conta padrão" mesmo sob corrida; store.ts expõe `definirContaPadrao(contaId)` via `pb.send`. (3) ContasCarteiras.tsx: badge azul "Padrão" (IconCheckCircle) na conta padrão + botão de ação (IconCheckCircle) nas contas ATIVAS não-padrão para "Definir como conta padrão" (desabilitado na que já é padrão; spinner enquanto salva; respeita o cofre admin/gerente porque a rota valida o papel). Assim o operador reatribui o destino da receita de OS pela própria tela, sem Admin UI do PB. Commit F-223 (rodada 2). Validado: `node --check` fin_routes.pb.js OK; `tsc -b` exit 0; `vitest run` store 71/71 (novos testes: "marcar B desmarca A" com invariante de 1 padrão + pbToConta padrao=false quando ausente). RUNTIME em cópia descartável (PB 0.39.4, :8095): estado inicial Banco Inter padrao=true; `POST /contas/padrao {Nubank}` → Nubank padrao=true e Inter padrao=false (exatamente 1 padrão); guard profissional 403. Arquivos: cleanos/pb/pb_hooks/fin_routes.pb.js, cleanos/web/src/lib/financeiro/types.ts, cleanos/web/src/lib/collections.ts, cleanos/web/src/lib/financeiro/store.ts, cleanos/web/src/lib/financeiro/store.test.ts, cleanos/web/src/pages/painel/financeiro/ContasCarteiras.tsx. Mantido status `verificado` — reviewer re-verifica o novo commit.
- (reviewer) Rodada 2 — verificação: CONFIRMADO `verificado`. Li fin_routes.pb.js (commit 11941c5): (1) a rota /contas/padrao, dentro de `$app.runInTransaction`, varre `findRecordsByFilter("fin_contas","padrao = true")`, desmarca TODAS as outras (`id !== contaId`) e marca só a alvo no MESMO txApp → invariante "no máximo 1 padrão" imune a corrida (a varredura por segurança cobre até estados legados com >1 padrão). (2) `pbToConta` mapeia `padrao: rec.padrao ?? false` → contas antigas sem o campo lidas como false (migration 1700000016_conta_padrao.js adiciona o BoolField e pina a 1ª ativa como padrão). (3) Guard admin/gerente na rota (requireAuth+requireAdminOrGerente); UI mostra o botão só em `c.ativo` e desabilita na que já é padrão — o gate de papel é server-side (não-admin → 403 → banner), a UI não oculta o botão mas o painel é cofre COFRE_FIN (cosmético). (4) Coerência com o hook de receita de OS: o hook prefere `ativo && padrao` (limit 1) com fallback à 1ª ativa; trocar a padrão redireciona a receita FUTURA para a nova conta e NÃO toca lançamentos passados (o hook só decide o destino na criação do lançamento) — correto. `tsc -b` exit 0 e `vitest run` 314/314 reconferidos. Sem regressão.

## F-224 | categoria: funcional | severidade: média | status: verificado
- Tela: cleanos/web/src/pages/painel/financeiro/Lancamentos.tsx:305-317 e :597; store.ts:288-293 (duplicateLancamento), :242-249 (createLancamento), :188 (VisaoGeral receitaViaOs)
- Passos: 1) Financeiro → Lançamentos. 2) Num lançamento origem "Via OS", clicar "Copiar"/"Repetir".
- Esperado: não deveria fabricar um 2º recebimento fantasma da mesma OS (ou a cópia deveria virar lançamento Manual sem vínculo/duplo crédito).
- Observado: "Copiar"/"Repetir" de um lançamento via_os cria um 2º lançamento que MANTÉM o chip "Via OS" e o VÍNCULO COM A OS → receita e saldo em dobro, sem guard. Classe: double-count de cálculo financeiro.
- (navegador) CONFIRMADO empírico em produção: "Copiar" no lançamento "OS #000259 - Colchão casal" criou "...(cópia)" mantendo Via OS + vínculo à OS #000259; PREVISTAS R$880/4 → R$1.040/5 (+R$160 duplicado). Cópia deletada (dado limpo). Evidências ss_9395xvq9m, ss_6114vuzle, ss_76984r8wb.
- (resolvedor) Correção: causa raiz = `duplicateLancamento`/`repeatLancamento` (store.ts) copiavam TODOS os campos do original, inclusive origem `via_os` + vínculo `os_id`/`os_numero`, então a cópia era contada de novo em "receita Via OS" (VisaoGeral :188, Relatorios :318) → double-count. Fix: helper `desvincularOsSeViaOs` aplicado nas DUAS funções — quando a origem é `via_os`, a cópia nasce como `manual` com `osId`/`osNumero`/`clienteNome`/`servicoNome` limpos (preserva a intenção de UX: copiar como lançamento manual, sem crédito duplicado da OS). Origem `manual` é no-op. Commit F-224. Validado: `tsc -b` exit 0. Arquivo: cleanos/web/src/lib/financeiro/store.ts.
- (reviewer) Verificação: commit e91facb ok. `desvincularOsSeViaOs` (store.ts:294) é aplicado nas DUAS únicas funções de cópia — `duplicateLancamento` (:335) e `repeatLancamento` (:352); grep confirmou que os únicos call-sites de cópia na UI (Lancamentos.tsx:295 repeat, :309 duplicate) passam por elas, sem caminho paralelo que preserve o vínculo. Quando origem='via_os' a cópia vira origem='manual' com osId/osNumero/clienteNome/servicoNome=undefined → não é mais contada em "receita Via OS" (VisaoGeral/Relatorios), matando o double-count. Origem='manual' retorna o input intacto (no-op) → cópia de lançamento manual comum segue idêntica ao comportamento anterior. VERIFICADO.

## F-601 | categoria: funcional | severidade: média | status: corrigido
- Tela: Clientes (busca) — app Flutter, painel admin. Provável repositório: `cleanos/flutter/lib/painel/data/` ou `lib/core/repositories/` (busca server-side via filtro PocketBase). Evidência: screenshot `e2e_13_qateste.png` (rodada E2E 2026-07-02).
- Passos: 1) Ter um cliente cujo nome contenha 2+ palavras (ex.: "QA Teste Silva"). 2) No campo de busca de Clientes, digitar um termo com espaço, ex.: "QA Teste".
- Esperado: encontrar o cliente (match parcial por qualquer parte do nome).
- Observado: termo com espaço ("QA Teste") retorna "Nenhum cliente encontrado", enquanto um termo de 1 palavra ("Silva") localiza o mesmo cliente. A busca falha quando o query contém espaço — provável filtro PocketBase montado sem escapar/dividir o espaço (o espaço vira operador ou o `like` não casa a string inteira com espaço).
- (resolvedor) Correção: causa raiz confirmada em `clienteSearchFilter` (`cleanos/flutter/lib/painel/data/painel_filters.dart`) — o termo inteiro virava um único literal `nome ~ 'QA Teste' || sobrenome ~ 'QA Teste' || …`, e como o painel divide "QA Teste Silva" em `nome`="QA" + `sobrenome`="Teste Silva", nenhum campo isolado contém a string "QA Teste", então o `~` nunca casava. Fix mínimo: tokenizar o termo por espaços e exigir que CADA palavra case em algum campo (AND por token, OR entre campos) — espelha o `fullName=[nome,sobrenome].join(' ')` + `includes(termo)` do React (`Clientes.tsx`), mantendo server-side e binding seguro via `pbStringLiteral`. Validação: teste `test/painel/clientes_busca_test.dart` (filtro gerado + fake que aplica o filtro à la PocketBase, com ciclo vermelho provando que o filtro antigo falhava); `dart analyze` limpo. Gate consolidado (flutter test) roda serializado depois.

## F-602 | categoria: visual | severidade: baixa | status: aberto
- Tela: Nova OS / Editar OS — dropdown de Hora. App Flutter, painel admin. Evidência: rodada E2E 2026-07-02 (dropdown Hora, disponibilidade 08–17).
- Passos: 1) Abrir Nova OS (ou Editar OS). 2) Abrir o dropdown de Hora e selecionar 10:00.
- Esperado: o item/label exibir "10".
- Observado: o dropdown exibe "1" em vez de "10" — o "0" é cortado (clipping na largura do item). O valor real salvo está correto (10:00); o problema é exclusivamente cosmético de exibição.

## F-603 | categoria: visual | severidade: baixa | status: corrigido
- Tela: cleanos/flutter/lib/painel/ordens/os_form.dart:533 (Row dos botões de ação — rodapé Cancelar/Salvar). App Flutter, painel admin.
- Passos: 1) Abrir o form de OS (Nova OS ou Editar OS) numa largura de tela muito estreita (< ~366 px de largura útil). 2) Observar a linha de botões Cancelar/Salvar no rodapé.
- Esperado: os botões cabem sem overflow.
- Observado: RenderFlex overflow de ~5,5 px no Row do rodapé de botões abaixo de ~366 px de largura. Pré-existente (não introduzido pelo fix do F-602); só se manifesta em telas muito estreitas (aparelhos muito pequenos ou split-screen). Descoberto ao validar o fix de layout do F-602 (empilhamento Data+Hora). Sugestão de correção futura: permitir wrap dos botões, reduzir spacing, ou usar Expanded/Flexible no Row de ações abaixo de um breakpoint estreito.
- (resolvedor) Correção: causa-raiz = `Row(mainAxisAlignment: end)` com os dois `ClxButton` de largura intrínseca + `SizedBox(x3)` estourava ~5,5px quando a largura útil caía abaixo de ~366px. Fix mínimo em `os_form.dart:534-575`: o rodapé virou `LayoutBuilder` que preserva o layout ORIGINAL (a mesma `Row` alinhada à direita, aparência e altura idênticas) em largura normal e só troca por um `Wrap(alignment: end)` — que deixa o "Salvar" descer p/ a 2ª linha, rótulos preservados, sem corte — abaixo do breakpoint `constraints.maxWidth < 366`. IMPORTANTE: um `Wrap` incondicional (1ª tentativa) perturbava a altura do form no `Dialog` de 620px e fazia o campo de Data cair numa região clipada de scroll, QUEBRANDO o teste `ordens_screen_test.dart` ("Nova OS: fluxo completo" — o date picker não abria mais). O gate por `LayoutBuilder` mantém o caminho largo byte-idêntico à base → sem regressão. Arquivos: cleanos/flutter/lib/painel/ordens/os_form.dart; teste novo test/painel/os_form_footer_overflow_test.dart. Branch `fix/qa-os-form-narrow` (a partir de feat/flutter-rewrite; NÃO mergeada). Validação: `dart analyze lib/painel/ordens/` LIMPO; `flutter test test/painel/os_form_footer_overflow_test.dart` verde (form a 340px: sem `RenderFlex overflowed`, rótulos "Cancelar"/"Salvar" inteiros/`_clipped==false`); teste NÃO-tautológico (revertendo p/ a `Row` incondicional ele falha com overflow); suíte `flutter test test/painel/` = **204/204** (inclui o `ordens_screen_test` que a 1ª tentativa quebrava); `flutter build apk --target-platform android-x64 -t lib/main_android.dart --debug` = **✓ Built app-debug.apk** (sem overflow). Commit desta rodada na branch (hash no handoff).

## F-740 | categoria: visual | severidade: média | status: corrigido
- Tela: cleanos/flutter/lib/core/design/widgets/toast.dart:24-28 (ramo `ToastType.warning` do `showClxToast`). App Flutter, tema ESCURO.
- Passos: 1) App em tema escuro. 2) Disparar um toast de aviso (`showClxToast(context, '...', type: ToastType.warning)`). 3) Ler o texto do toast.
- Esperado: texto legível sobre o fundo do toast (contraste WCAG AA ≥ 4.5:1).
- Observado: REGRESSÃO introduzida no commit MD3 5231813. O ramo warning teve o texto/ícone trocados de `Colors.black87` → `Colors.white` (diff toast.dart) para arrumar o tema CLARO (fundo `clx.warning` = `#B45309`, escuro → branco OK). Mas o fundo é `clx.warning`, que no tema ESCURO é `#FBBF24` (âmbar claro, cleanox_colors.dart:199). Branco sobre `#FBBF24` dá contraste ≈ 1,6:1 — ilegível e pior que o `black87` anterior (que dava ≈ 12:1 no escuro). O fix corrigiu o claro e quebrou o escuro. Correção sugerida: escolher a cor do texto por brilho do fundo (ex.: `ThemeData.estimateBrightnessForColor(clx.warning)` → preto no âmbar claro / branco no âmbar escuro), em vez de fixar branco.
- (resolvedor) Correção: causa-raiz = cor do texto/ícone do warning fixada em `Colors.white`, ignorando que `clx.warning` inverte de tom por tema. Fix mínimo em toast.dart:16-33 — só o warning passa a derivar a cor do texto/ícone do BRILHO do fundo real via `ThemeData.estimateBrightnessForColor(clx.warning)`: fundo escuro (`#B45309`, tema CLARO) → branco; fundo âmbar-claro (`#FBBF24`, tema ESCURO) → `black87`. Fundo NÃO tocado; success/error/info seguem brancos (aparência preservada — não consolidei porque no escuro seus fundos são claros e a lógica por brilho MUDARIA a aparência atual deles). Arquivos: cleanos/flutter/lib/core/design/widgets/toast.dart; teste novo test/core/toast_test.dart. Branch `fix/qa-md3-followups` (a partir de feat/flutter-rewrite; NÃO mergeada). Validação: `dart analyze lib/core/design/` LIMPO; `flutter test test/core/toast_test.dart` 4/4 verdes — assevera texto branco no CLARO e `black87` no ESCURO (widget test pumpa o toast real nos dois temas) + o contraste WCAG. CONTRASTE medido: tema CLARO `#B45309`+branco = **5,0:1** (≥4.5 ✓); tema ESCURO `#FBBF24`+`black87` (composto sobre o fundo) = **~10,5:1** (≥4.5 ✓). Ambos AA.

## F-700 | categoria: visual | severidade: baixa | status: corrigido
- Tela: Login (https://cleanox.wenox.com.br/#/login), tema escuro. Evidência: screenshot ss_9660he83a (zoom ss da região do form).
- Passos: 1) Deslogar / abrir a tela de login. 2) Observar o botão primário "Entrar" (CTA principal do formulário).
- Esperado: por consistência com a migração MD3 (botões pill / StadiumBorder), o CTA primário deveria ter cantos totalmente arredondados (raio ≈ metade da altura).
- Observado: o botão "Entrar" renderiza como retângulo com raio moderado (~10–12 px numa altura ~40–48 px), NÃO um StadiumBorder pleno. A fonte Sora e o título "CleanOS" renderizam corretos (glifos distintos do system font); só a forma do botão diverge do padrão pill esperado. Observação: confirmação cruzada com botões in-app ficou pendente porque o login com as credenciais fornecidas (admin@cleanox.local / cleanox123) falhou nesta produção ("E-mail ou senha inválidos.") — credencial .local não autentica no domínio wenox.com.br.
- (reviewer) Causa-raiz: o botão "Entrar" usa o widget `ClxButton` (login_screen.dart:144), e o próprio `ClxButton` NÃO é pill — ele é um `Material`+`InkWell`+`Container` custom que aplica `ClxRadii.rMd` (=10 px) em clx_button.dart:76,79,87 (token `md=10` em tokens.dart:19). NÃO há shape/borderRadius local no botão de login: a divergência vem do próprio componente do design system. O StadiumBorder do MD3 só está aplicado aos temas de botão *cru* do ThemeData (filledButtonTheme/outlinedButtonTheme/textButtonTheme → `StadiumBorder()` em theme.dart:293,302,310), que o login não usa. Veredito: **regressão/inconsistência do design system** (não intencional): a migração para pill foi aplicada aos button themes do ThemeData mas o `ClxButton` — componente que todas as telas de fato usam — ficou de fora, contrariando o comentário em theme.dart:287 que declara StadiumBorder como padrão MD3. Consequência: TODO `ClxButton` do app (não só o login) renderiza com 10 px, não só a tela de login. Fix da causa-raiz é trocar `ClxRadii.rMd`→`ClxRadii.rPill` nas 3 linhas de clx_button.dart, não mexer no login_screen.dart.
- (navegador) Rodada 2 — cross-check pós-login (teste@testando.com): os botões primários INLINE/toolbar do app RENDERIZAM como pill/StadiumBorder pleno — "Nova OS" (Ordens), "Novo lançamento" (Financeiro/Lançamentos), "Atualizar" (Avaliações), e os filter-chips ("Todas", "Todos/Receitas/Despesas", sub-abas de Relatórios) são pills. Ou seja, a migração MD3 de pills FUNCIONA. O padrão observado é: botões primários inline = pill; CTAs de largura total / bloco (login "Entrar", quick-access do Dashboard "+ Nova OS"/"+ Novo Cliente"/"Ver Agenda"/"Financeiro") = retângulo arredondado (~10-12px) de forma CONSISTENTE. Portanto o "Entrar" não-pill é provavelmente uma VARIANTE de design intencional (FilledButton block), não uma regressão. Reclassificado para precisa-decisão: confirmar com design se o CTA de bloco deve ou não ser pill. Não é bug de contraste/fonte/overflow — Sora e título "CleanOS" OK.
- (resolvedor) Correção: DECISÃO DO DONO aplicada — os botões de BLOCO (`ClxButton`) passam a ser PILL, consistentes com os button themes MD3 do ThemeData (`StadiumBorder` em theme.dart:293,302,310) e com os chips/inline. Causa-raiz (do reviewer, confirmada): o `ClxButton` — componente que TODAS as telas usam (incl. o CTA "Entrar" do login) — aplicava `ClxRadii.rMd` (=10px) e ficou de fora do upgrade pill. Fix mínimo: `ClxRadii.rMd`→`ClxRadii.rPill` (raio 100, já existente em tokens.dart:28) nas 3 linhas do componente compartilhado — clx_button.dart:76 (Material.borderRadius), :79 (InkWell.borderRadius), :87 (BoxDecoration.borderRadius). NÃO toquei login_screen.dart nem outras telas: a mudança é no componente compartilhado, propaga sozinha (app-wide). Full-width (`expand:true`) mantém as pontas semicirculares (pill MD3 correto) sem cortar label nem quebrar padding/altura (o label já é `Flexible`+ellipsis; a altura segue o toque mínimo de 48dp). Arquivos: cleanos/flutter/lib/core/design/widgets/clx_button.dart; teste novo test/core/clx_button_test.dart. Branch `fix/qa-md3-followups` (a partir de feat/flutter-rewrite; NÃO mergeada). Validação: `dart analyze lib/core/design/` LIMPO; `flutter test test/core/ test/painel/` = **286/286 verdes** (nenhum teste existente quebrou com a mudança app-wide de shape); os 2 widget-tests novos assertam o raio da decoração ≥ metade do toque mínimo (48dp) — NÃO-tautológico: com o antigo `rMd`=10 falham (10 < 24), com `rPill` passam; `flutter build apk --target-platform android-x64 -t lib/main_android.dart --debug` = **✓ Built app-debug.apk** (sem overflow). Commit desta rodada na branch (hash no handoff).

## F-741 | categoria: usabilidade | severidade: média | status: verificado
- Reporter: DONO DO PRODUTO (relato verbatim: "o módulo todo do Financeiro precisa ser otimizado no APK: o FILTRO está ocupando muito espaço de tela, e os painéis são FIXOS."). Superfície: APK/mobile (largura de celular, <~600 px). Módulo: Financeiro do painel (Flutter).
- Tela (causa-raiz em código):
  - **Padrão comum a todas as telas — header/toolbar de filtro FIXO (não-rolável) acima de `Expanded(body)`:** cada tela monta `Column([_Header/_Toolbar (+_Kpis), Expanded(body)])`, então a faixa de filtro/período/KPIs NUNCA rola pra fora — em tela de celular ela come viewport permanentemente. Ocorrências: `lancamentos/fin_lancamentos_screen.dart:218-243` (Column: `_Toolbar` + `_Kpis` + `Expanded` — PIOR caso: barra de filtro E grade de KPI ambas fixas acima da lista), `fin_visao_geral_screen.dart:68-90`, `fin_relatorios_screen.dart:138-172`, `fin_contas_pagar_receber_screen.dart:170-178`.
  - **Toolbar de filtro sempre 100% expandida, paddings fixos, sem colapso mobile:** `lancamentos/fin_lancamentos_screen.dart:441-566` (`_Toolbar`) — `padding` fixo `fromLTRB(x6,x4,x6,x3)` (:474) e empilha 3 blocos verticais: Row período+"Novo lançamento" (:486-496), busca em UMA linha só pra ela (:498-522), e `Wrap` de chips/menus tipo+status+categoria+conta (:524-562). SEM `LayoutBuilder`/`MediaQuery` — layout idêntico a 360 px e a 1200 px; empilhado dá ~200 px de altura fixa. `fin_relatorios_screen.dart:206-277` (`_Header`) repete o padrão (título+Exportar+período Row :219-238 + `Wrap` de filtros :240-274, padding fixo).
  - **Grade de KPI fixa acima da lista:** `lancamentos/fin_lancamentos_screen.dart:389-437` (`_Kpis`) — `FinKpiGrid` em 2 colunas no mobile (fin_common.dart:237-241) → 2 linhas ≈ 190 px, entre a toolbar e a lista, sem rolar. Somado à toolbar, ~400 px de faixa fixa antes do 1º lançamento aparecer num viewport de ~640 px.
  - **Dimensões fixas em componentes de painel/filtro:** `FinPeriodSelector` com `minWidth: 132` (fin_common.dart:77) + dois `IconButton` de 48 dp ≈ 228 px fixos (fin_common.dart:52-95); tiles de ação rápida `width: 92` (fin_visao_geral_screen.dart:377).
- Passos: 1) Abrir o APK numa largura de celular (<~600 px). 2) Ir em Financeiro → Lançamentos (ou Relatórios / Visão geral). 3) Observar quanto da tela some antes do conteúdo útil (lista de lançamentos / gráficos) aparecer.
- Esperado: filtro compacto/colapsável no mobile (ex.: bottom-sheet ou seção expansível) e painéis (filtro + KPIs) que ROLAM junto com o conteúdo — priorizando o conteúdo útil. Barra de filtro não deveria consumir ~1/3+ do viewport de forma fixa.
- Observado: o relato do dono se confirma em código — a faixa de filtro/período/KPI é um irmão FIXO acima de `Expanded(body)` (nunca rola fora), a toolbar de Lançamentos/Relatórios está sempre totalmente expandida com paddings/empilhamento fixos e sem breakpoint de mobile, e a grade de KPIs some junto ~400 px de altura fixa antes da lista. O módulo tem responsividade PARCIAL (FinKpiGrid 2/3/4 cols via LayoutBuilder; `_ResponsiveRow`; Wrap nos filtros), mas ela não ataca a faixa fixa nem o always-expanded — que são exatamente o que desperdiça viewport estreito.
- Sugestão de correção: (1) no mobile (breakpoint via `MediaQuery`/`LayoutBuilder`, ex.: <600 px), colapsar a toolbar de filtro num botão "Filtros" que abre `showModalBottomSheet` (ou `ExpansionTile`) — o padrão JÁ EXISTE no módulo: a tela Contas usa um toggle `_showFilters` (`fin_contas_pagar_receber_screen.dart:174-176`), só falta replicar em Lançamentos/Relatórios/Visão geral; (2) mover a grade de KPIs e a barra de período pra DENTRO do `ListView`/scroll (como cabeçalho rolável via `SliverToBoxAdapter`/`CustomScrollView`) em vez de irmão fixo acima do `Expanded`, para que rolem junto e liberem viewport; (3) reduzir os paddings verticais fixos (`fromLTRB(x6,x4,x6,x3)`) sob o breakpoint estreito; (4) remover dimensões fixas onde couber (ex.: `FinPeriodSelector minWidth 132` → `Flexible`/menor no mobile).
- Anti-duplicata: distinto de F-602/F-603 (esses são `os_form.dart` — form de OS), F-740 (contraste de toast) e F-700 (shape de botão). Nenhum finding prévio cobre o desperdício de viewport da faixa de filtro/KPI do módulo Financeiro no mobile.
- (resolvedor) Correção: causa-raiz confirmada = faixa filtro/período/KPI era irmã FIXA acima de `Expanded(body)` + toolbar sempre 100% expandida sem breakpoint mobile. Introduzido breakpoint `finIsMobile(context)` (`< 600 px`, via `MediaQuery.sizeOf`) e o layout `< 600 px` foi reescrito para rolar tudo junto e colapsar o filtro; `≥ 600 px` (desktop/tablet) permanece IDÊNTICO. Arquivos/linhas:
  - `fin_common.dart`: `kFinMobileBreakpoint`/`finIsMobile` novos (:60-72); `FinFiltrosToggle` (botão "Filtros" reutilizável, toque ≥48 dp, ponto de filtro ativo) novo (:100-159); `FinPeriodSelector` ganhou `expand` — no mobile o rótulo vira `Expanded` (elipse) e ocupa a largura total sem estourar (o `minWidth:132` só vale no desktop) (:52-135).
  - `lancamentos/fin_lancamentos_screen.dart`: PIOR caso resolvido — no mobile `build` monta um `CustomScrollView` (`_mobileBody`) com o cabeçalho (toolbar + `_Kpis(mobile:true)`) num `SliverToBoxAdapter` rolável e a lista/estados em `_bodySlivers` (`SliverList`/`SliverFillRemaining`), em vez de `Column([_Toolbar fixo, _Kpis fixo, Expanded(lista)])`; `_Toolbar` ganhou caminho `_mobile` (período full-width + linha "Novo lançamento"/"Filtros" + busca/chips colapsados atrás do toggle `_showFilters`, default fechado) preservando o `_desktop` original; `_Kpis(mobile)` zera o padding próprio dentro do scroll. Estado `_showFilters` novo (:47-49).
  - `fin_relatorios_screen.dart`: `_MobileHeader` rolável (período full-width + "Filtros" colapsando os `_RepFilter` extraídos p/ `_RelFiltersWrap` + "Exportar") entra como `leadingChildren` do `_Body` (ListView) no mobile; header fixo `_Header` só no desktop; `_showFilters` novo. Padding do corpo x6→x4 no mobile.
  - `fin_visao_geral_screen.dart`: `_MobileHeader` (título + período full-width) vira `leadingChildren` do `_Body` no mobile em vez de `_Header` fixo; padding x6→x4 no mobile.
  - `fin_contas_pagar_receber_screen.dart`: NÃO tocado — é a referência (já tinha `_showFilters`/KPIs no scroll); herda o seletor de período mais estreito no mobile de graça.
  Validação: `dart analyze lib/painel/financeiro/ + test novo` LIMPO; `flutter build apk --target-platform android-x64 -t lib/main_android.dart --debug` OK (assinatura release indisponível → debug); teste novo `test/painel/fin_mobile_layout_test.dart` a 360×760 confirma toolbar colapsada (busca escondida, botão "Filtros" presente), KPIs dentro de um `CustomScrollView` e SEM overflow no frame estável (relayout forçado a 360 px); suíte `flutter test test/painel/` verde (203 testes). Commit único desta rodada na branch `fix/qa-financeiro-mobile` (hash no handoff). Não mergeado, não deployado.
- (resolvedor) observação: com dados reais e ≤360 px há um overflow PRÉ-EXISTENTE e DISTINTO nas LINHAS da lista (`_DayHeader` e chips de `_LancamentoRow`/`ClxChip`) — widgets não tocados por este fix e fora do escopo de F-741 (que é a faixa de filtro/KPI). Só aparece em frames transitórios do fake-async no teste; o frame estável é limpo. Vale um finding próprio para densidade de linha no mobile.
- (reviewer) Verificação: VERIFICADO (READ-ONLY, commit `5a7db0f`, branch `fix/qa-financeiro-mobile`, NÃO mergeada). (1) Mobile <600 px REALMENTE corrigido nas 3 telas: Lançamentos `build` desvia p/ `_mobileBody` = `CustomScrollView` com cabeçalho (toolbar + `_Kpis(mobile:true)`) num `SliverToBoxAdapter` rolável + `_bodySlivers` (`SliverList.builder`/`SliverFillRemaining`) — a faixa de filtro/KPI deixou de ser irmã fixa acima de `Expanded`; `_Toolbar._mobile` colapsa busca+chips atrás de `FinFiltrosToggle` com `_showFilters=false` default (fin_lancamentos_screen.dart:250,264-299,669-704). Relatórios e Visão geral movem o header p/ `leadingChildren` do `ListView` do `_Body` via `_MobileHeader` rolável, filtros colapsáveis (fin_relatorios_screen.dart:153-192,357-430; fin_visao_geral_screen.dart:71-94,138-159). Breakpoint = `finIsMobile` (`MediaQuery.sizeOf(context).width < 600`, fin_common.dart:55-60); não afeta ≥600. (2) Desktop ≥600 PRESERVADO: cada `build` faz `if (mobile) return ...;` e o caminho `Column([_Header/_Toolbar(+_Kpis), Expanded(body)])` original segue intacto; `fin_screens_test.dart`/`fin_paridade_test.dart` rodam via `pumpPainel` a 1400×900 (painel_test_helpers.dart:67,129) → `finIsMobile=false` → exercitam o caminho desktop; suíte de 203 testes verde. (3) SEM overflow novo: `FinPeriodSelector.expand` usa `Expanded(label)` dentro de pai bounded (`SizedBox(width:double.infinity)`) com `mainAxisSize.max` e elipse (fin_common.dart:92-123); toolbar mobile Row = `Expanded(ClxButton)` + `FinFiltrosToggle(mainAxisSize.min)`; slivers bem-formados (`hasScrollBody:false` nos `SliverFillRemaining`). (4) Teste novo NÃO-tautológico: assevera `CustomScrollView` presente, KPI 'Receitas realizadas' dentro do scroll, botão 'Filtros' presente + `searchField` escondido, e tocar 'Filtros' REVELA a busca; `expectStableNoOverflow` força relayout a 360 px e exige `takeException()==null` (fin_mobile_layout_test.dart:41-54,76-127). (5) Contrato íntegro: nenhum finding deletado/sobrescrito — o commit só ADICIONOU entradas e a nota `(resolvedor) Correção` de F-741 é coerente com o diff. Observação de processo (não bloqueia): este commit também promoveu F-011/F-012/F-220..F-224 de `corrigido`→`verificado` com notas de reviewer anexadas (bundle de múltiplos workers no mesmo arquivo/commit) — aditivo, sem perda de dados. (6) Overflow pré-existente do resolvedor CONFIRMADO real e fora do escopo → registrado como F-742.

## F-742 | categoria: visual | severidade: baixa | status: corrigido
- Tela: `cleanos/flutter/lib/painel/financeiro/lancamentos/fin_lancamentos_screen.dart:944-968` (`_DayHeader`, o cabeçalho de dia da lista de Lançamentos). Widget PRÉ-EXISTENTE, NÃO tocado pelo fix de F-741 (commit `5a7db0f` só moveu o call-site, fin_lancamentos_screen.dart:334). Levantado pelo resolvedor de F-741 e confirmado na verificação.
- Passos: 1) Abrir Financeiro → Lançamentos numa largura muito estreita (≤360 px, ou APK pequeno/split-screen). 2) Ter um dia com total de valor alto (ex.: `R$ 1.234.567,89`). 3) Observar o cabeçalho do dia.
- Esperado: data + contagem + total do dia cabem na linha sem overflow.
- Observado: o `Row` do `_DayHeader` tem três `Text` NÃO protegidos (`formatDateOnlyBr` da data + `'N lançamentos'` + `formatSignedValue(total)`) com um `Spacer()` entre a contagem e o total; nenhum é `Flexible`/`Expanded` nem tem `ellipsis`. Com padding horizontal fixo `x6` (24 px cada lado) e valores de total longos ou largura <360 px a soma dos textos excede a largura → `RenderFlex overflowed`. Só se manifesta com dados reais/valores altos em viewport muito estreito (no teste aparece apenas em frames transitórios do fake-async; o frame estável a 360 px com valores pequenos é limpo). DISTINTO de F-603 (overflow em `os_form.dart`) e de F-741 (faixa de filtro/KPI). Correção sugerida (rodada futura): envolver a contagem em `Flexible`+ellipsis e/ou reduzir o padding horizontal sob o breakpoint estreito; a mesma proteção vale para o `Text` de valor de `_LancamentoRow` (fin_lancamentos_screen.dart:1071, fora do `Expanded`) se reproduzir com valores muito longos.
- (resolvedor) Correção: causa-raiz = `Row`s com `Text` inflexíveis. (1) `_DayHeader` (agora fin_lancamentos_screen.dart:742-766 nesta branch, base feat/flutter-rewrite pré-F-741) — a contagem virou `Expanded` (substitui `Text` + `Spacer`, mesmo visual: empurra o total pra direita) com `maxLines:1`/`softWrap:false`/`ellipsis`, e o total virou `Flexible` com ellipsis + `textAlign.end`; a Row nunca estoura mesmo com `+R$ 123.456.790,01` a 320 px. (2) Valor do `_LancamentoRow` (fin_lancamentos_screen.dart:869) envolto em `Flexible` + `ellipsis` + `textAlign.end`. Aparência em larguras comuns preservada (Expanded/Flexible só encolhem quando falta espaço). NÃO usei `finIsMobile` (é código do F-741, ausente nesta base) — a proteção é puro layout, sem breakpoint. Arquivos: cleanos/flutter/lib/painel/financeiro/lancamentos/fin_lancamentos_screen.dart (+ 2 builders `@visibleForTesting` no fim do arquivo, só p/ o teste isolar os widgets privados); teste novo test/painel/fin_lancamentos_overflow_test.dart. Branch `fix/qa-md3-followups` (a partir de feat/flutter-rewrite; NÃO mergeada). Validação: `dart analyze lib/painel/financeiro/` LIMPO; `flutter test test/painel/ test/core/` = **282/282 verdes** (inclui os 2 testes novos de overflow + os 4 do F-740); `flutter build apk --target-platform android-x64 -t lib/main_android.dart --debug` = **✓ Built app-debug.apk** (sem overflow no build). O teste do `_DayHeader` é NÃO-tautológico: revertendo o fix ele FALHA com `RenderFlex overflowed`; com o fix passa. O `_LancamentoRow` é validado a 420 px (no-overflow) + assert estrutural de que o valor tem `Flexible` ancestral e `overflow: ellipsis`.
- (resolvedor) observação: DISTINTO e FORA do escopo de F-742 — a 320 px o `ClxChip` (ex.: `OrigemChip`) tem overflow INTERNO próprio (Row de ícone+label em clx_chip.dart:32, ~20 px) quando a célula do `Wrap` fica minúscula. É pré-existente e não é o `Text` de valor que F-742 cobre; vale um finding próprio p/ o `ClxChip` em larguras extremas (não toquei).

## F-743 | categoria: visual | severidade: baixa | status: corrigido
- Tela: `cleanos/flutter/lib/core/design/widgets/clx_chip.dart:32-47` (o `Row` interno do `ClxChip`). Componente compartilhado do design system; superfície observada: chips do `_LancamentoRow` (`OrigemChip`/`StatusLancamentoChip`/etc. no `Wrap` em fin_lancamentos_screen.dart:868-879). App Flutter. Levantado como observação do resolvedor de F-742 e confirmado na verificação READ-ONLY.
- Passos: 1) Abrir Financeiro → Lançamentos numa largura muito estreita (≤320 px, APK pequeno/split-screen). 2) Ter linhas cujo chip (ex.: `OrigemChip`) tenha label mais longo que a célula do `Wrap` disponível. 3) Observar o chip.
- Esperado: o label do chip elipsa/encolhe (ou o chip cede) sem estourar.
- Observado: o `Row` interno do `ClxChip` usa `mainAxisSize.min` com um `Text` de label SEM `Flexible`/`Expanded` e SEM `overflow: ellipsis` (clx_chip.dart:39-46). Quando o `Wrap` pai dá ao chip uma `maxWidth` menor que a largura intrínseca do conteúdo (ícone + label), o `Row` interno estoura → `RenderFlex overflowed` (~20 px observado a ≤320 px). É INTERNO ao chip (distinto de F-742, que protege o `Text` de valor do `_LancamentoRow`) e PRÉ-EXISTENTE (o `ClxChip` não foi tocado por F-741/F-742). Como `ClxChip` é usado em todo o app (grupos de serviço, tags, badges), o risco é latente em qualquer container estreito com label longo. Correção sugerida (rodada futura): envolver o `Text` do label em `Flexible` + `overflow: TextOverflow.ellipsis`/`maxLines:1` dentro do `Row` do `ClxChip`. Anti-duplicata: distinto de F-742 (Text de valor / `_DayHeader`), F-603 (`os_form.dart`) e F-741 (faixa de filtro/KPI).
- (resolvedor) Correção: causa-raiz = `Text` do label do `ClxChip` sem `Flexible`/ellipsis dentro de um `Row` `mainAxisSize.min`, então sob `maxWidth` apertado o Row estourava. Fix mínimo: o `Text` do label virou `Flexible(child: Text(..., maxLines:1, softWrap:false, overflow: TextOverflow.ellipsis))` (clx_chip.dart:39-51), mantendo `mainAxisSize.min` — o `Flexible` só encolhe o label quando FALTA espaço; em espaço sobrando o chip continua colado ao conteúdo (aparência de chips curtos inalterada). Arquivo: cleanos/flutter/lib/core/design/widgets/clx_chip.dart; teste novo test/core/clx_chip_test.dart. Branch `fix/qa-md3-followups` (a partir de feat/flutter-rewrite; NÃO mergeada). Validação: `dart analyze lib/core/design/` LIMPO; `flutter test test/core/ test/painel/` = **286/286 verdes**; os 2 widget-tests novos: (1) label longo num container de 120 px assevera `tester.takeException()==null` (SEM RenderFlex overflow) + o `Text` tem `overflow: ellipsis` e ancestral `Flexible` — NÃO-tautológico (sem o fix estoura); (2) chip curto sob constraint frouxa mantém `width < 300` (não estica). `flutter build apk --target-platform android-x64 -t lib/main_android.dart --debug` = **✓ Built app-debug.apk**. Commit desta rodada na branch (hash no handoff).
